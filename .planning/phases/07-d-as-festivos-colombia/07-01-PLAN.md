---
phase: 07-d-as-festivos-colombia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/src/seed-holidays.ts
  - packages/db/package.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Existe la tabla `holidays` en la BD con columnas date (date, unique) y name, y `prisma.holiday` está disponible en el cliente generado"
    - "El seed carga los 18 festivos nacionales de Colombia de 2026 y los 18 de 2027 (36 filas)"
    - "El seed es idempotente: re-correr `db:seed:holidays` no duplica filas (upsert por la fecha única)"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Modelo Prisma Holiday { id, date @unique @db.Date, name, createdAt } mapeado a holidays"
      contains: "model Holiday"
    - path: "packages/db/src/seed-holidays.ts"
      provides: "Script tsx idempotente que upserta los festivos CO 2026 y 2027 por la fecha única"
      contains: "upsert"
    - path: "packages/db/package.json"
      provides: "Script npm db:seed:holidays siguiendo el patrón tsx de los otros db:seed:*"
      contains: "db:seed:holidays"
  key_links:
    - from: "packages/db/src/seed-holidays.ts"
      to: "prisma.holiday"
      via: "prisma.holiday.upsert({ where: { date }, ... })"
      pattern: "holiday\\.upsert"
    - from: "packages/db/package.json"
      to: "packages/db/src/seed-holidays.ts"
      via: "script db:seed:holidays ejecuta tsx src/seed-holidays.ts"
      pattern: "seed-holidays"
---

<objective>
Crear la infraestructura de datos de festivos colombianos: un modelo Prisma `Holiday` mínimo (Colombia-only, sin countryCode ni tenantId), su migración/tabla `holidays`, y un script de seed idempotente (`seed-holidays.ts`) que upserta los 18 festivos nacionales de CO 2026 y los 18 de 2027. Esta es la base de datos que el gate de compliance (Plan 07-02) consultará vía `prisma.holiday.findFirst`.

Purpose: Sin la tabla `holidays` y su cliente generado, `ComplianceService` no puede consultar `this.prisma.holiday`. Este plan entrega el modelo + los datos y deja `prisma.holiday` disponible para que 07-02 typechequee y corra sus tests.
Output: Modelo `Holiday` en schema.prisma, migración `add_holidays` (tabla `holidays` + cliente regenerado), `packages/db/src/seed-holidays.ts`, y el script `db:seed:holidays` en package.json.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Convenciones de schema.prisma a imitar EXACTAMENTE para el nuevo modelo. -->
<!-- El id usa gen_random_uuid()/@db.Uuid; snake_case vía @map; @@map plural. -->

Patrón de otros modelos en packages/db/prisma/schema.prisma (p. ej. PromiseToPay, Contact):
```
id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
createdAt DateTime @default(now()) @map("created_at")
...
@@map("promises_to_pay")
```

Columnas de fecha civil (sin hora) usan @db.Date, p. ej. en Debt:
```
dueDate DateTime @map("due_date") @db.Date
```

Patrón de script tsx idempotente/backfill en packages/db/src/backfill-consents.ts:
```
import { PrismaClient } from "@prisma/client";
import { loadSeedEnv } from "./load-seed-env";
const prisma = new PrismaClient();
async function main(): Promise<void> {
  loadSeedEnv();
  // ... work with prisma ...
}
main()
  .catch((error: unknown) => { console.error(...); process.exit(1); })
  .finally(async () => { await prisma.$disconnect(); });
```

Scripts npm ya existentes en packages/db/package.json (imitar el patrón tsx):
```
"db:seed:align": "tsx src/seed-align.ts",
"db:backfill-consents": "tsx src/backfill-consents.ts",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelo Prisma Holiday + migración add_holidays (BLOCKING)</name>
  <files>packages/db/prisma/schema.prisma</files>
  <read_first>
    - packages/db/prisma/schema.prisma (modelos existentes como Contact/PromiseToPay para copiar convenciones: id gen_random_uuid()/@db.Uuid, @map snake_case, @@map plural, createdAt @default(now()) @map("created_at"); y la columna dueDate @db.Date de Debt como referencia de fecha civil)
    - packages/db/prisma/migrations/ (dirs timestamped como 20260708120000_add_contact_response_status; así se nombran las migraciones)
  </read_first>
  <action>
    Agregar al final de packages/db/prisma/schema.prisma un modelo nuevo `Holiday` mínimo, Colombia-only, SIN countryCode y SIN tenantId (los festivos nacionales son globales). Campos:
    - `id String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid`
    - `date DateTime @unique @map("date") @db.Date` — la fecha civil del festivo; @db.Date (sin hora), unique para que el upsert del seed sea idempotente por fecha.
    - `name String @map("name")` — nombre del festivo (contenido en español, p. ej. "Año Nuevo").
    - `createdAt DateTime @default(now()) @map("created_at")`
    - `@@map("holidays")`
    No agregar soft-delete (deletedAt) ni updatedAt. No agregar relaciones. Comentarios de código en INGLÉS (regla global del repo), aunque el resto del schema tenga prosa en español.

    Luego crear la migración y regenerar el cliente para que `prisma.holiday` exista (BLOCKING para el Plan 07-02, que typechequea contra `this.prisma.holiday`). Ejecutar desde la raíz del repo: `pnpm --filter @cobrai/db exec prisma migrate dev --name add_holidays`. Esto crea `packages/db/prisma/migrations/<timestamp>_add_holidays/migration.sql` y regenera el cliente. Si la BD de dev no está disponible para `migrate dev`, usar el fallback: `pnpm --filter @cobrai/db exec prisma db push` seguido de `pnpm --filter @cobrai/db exec prisma generate`. En cualquiera de los dos caminos, el resultado obligatorio es: la tabla `holidays` existe en la BD Y el cliente generado expone `prisma.holiday`. No editar a mano ningún cliente generado. No usar bloques de código fenced en el archivo; editar el schema in-place.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && grep -q 'model Holiday' packages/db/prisma/schema.prisma && grep -q '@@map("holidays")' packages/db/prisma/schema.prisma && node -e "const{PrismaClient}=require('@prisma/client');const p=new PrismaClient();if(typeof p.holiday?.findFirst!=='function'){console.error('prisma.holiday no disponible');process.exit(1)};console.log('prisma.holiday OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/prisma/schema.prisma` contiene `model Holiday` con `@@map("holidays")`, un campo `date DateTime @unique ... @db.Date` y un campo `name String`.
    - El modelo NO tiene columna countryCode ni tenantId (grep no encuentra `countryCode` ni `tenantId` dentro del bloque `model Holiday`).
    - Existe la migración `packages/db/prisma/migrations/<timestamp>_add_holidays/migration.sql` (camino migrate dev) O la tabla se aplicó vía `db push`; en ambos casos el cliente generado expone `prisma.holiday`.
    - El snippet node de verificación imprime `prisma.holiday OK` (el cliente generado expone `prisma.holiday.findFirst`).
  </acceptance_criteria>
  <done>El modelo `Holiday` existe con las convenciones del schema, la tabla `holidays` está en la BD y `prisma.holiday` está disponible en el cliente generado.</done>
</task>

<task type="auto">
  <name>Task 2: Script seed-holidays.ts idempotente (CO 2026 + 2027) + npm script db:seed:holidays</name>
  <files>packages/db/src/seed-holidays.ts, packages/db/package.json</files>
  <read_first>
    - packages/db/src/backfill-consents.ts (patrón exacto a imitar: import PrismaClient + loadSeedEnv, `new PrismaClient()`, async main() que llama loadSeedEnv(), y el bloque final main().catch(...).finally(() => prisma.$disconnect()))
    - packages/db/src/load-seed-env.ts (loadSeedEnv carga el .env del monorepo para la conexión de la BD)
    - packages/db/package.json (sección "scripts": copiar el patrón `"db:seed:align": "tsx src/seed-align.ts"` para el nuevo script)
  </read_first>
  <action>
    Crear `packages/db/src/seed-holidays.ts` siguiendo el patrón de backfill-consents.ts: importar `PrismaClient` de `@prisma/client` y `loadSeedEnv` de `./load-seed-env`, instanciar `const prisma = new PrismaClient()`, y un `async function main()` que primero llame `loadSeedEnv()`. Definir una constante hardcoded `HOLIDAYS_CO` (array de `{ date: string; name: string }`) con TODOS los festivos nacionales de Colombia de 2026 y 2027 — 18 por año, 36 en total. Los nombres van en español (son contenido/dato); los comentarios de código en inglés. Usar EXACTAMENTE esta lista curada (calendario oficial de festivos CO, incluye los movidos por la Ley Emiliani al lunes y los basados en Semana Santa):

    2026 (18):
    2026-01-01 Año Nuevo; 2026-01-12 Día de los Reyes Magos; 2026-03-23 Día de San José; 2026-04-02 Jueves Santo; 2026-04-03 Viernes Santo; 2026-05-01 Día del Trabajo; 2026-05-18 Ascensión del Señor; 2026-06-08 Corpus Christi; 2026-06-15 Sagrado Corazón de Jesús; 2026-06-29 San Pedro y San Pablo; 2026-07-20 Día de la Independencia; 2026-08-07 Batalla de Boyacá; 2026-08-17 Asunción de la Virgen; 2026-10-12 Día de la Raza; 2026-11-02 Día de Todos los Santos; 2026-11-16 Independencia de Cartagena; 2026-12-08 Inmaculada Concepción; 2026-12-25 Navidad.

    2027 (18):
    2027-01-01 Año Nuevo; 2027-01-11 Día de los Reyes Magos; 2027-03-22 Día de San José; 2027-03-25 Jueves Santo; 2027-03-26 Viernes Santo; 2027-05-01 Día del Trabajo; 2027-05-10 Ascensión del Señor; 2027-05-31 Corpus Christi; 2027-06-07 Sagrado Corazón de Jesús; 2027-07-05 San Pedro y San Pablo; 2027-07-20 Día de la Independencia; 2027-08-07 Batalla de Boyacá; 2027-08-16 Asunción de la Virgen; 2027-10-18 Día de la Raza; 2027-11-01 Día de Todos los Santos; 2027-11-15 Independencia de Cartagena; 2027-12-08 Inmaculada Concepción; 2027-12-25 Navidad.

    Para cada entrada, construir la fecha como un Date a MEDIANOCHE UTC del día civil — es decir `new Date(`${entry.date}T00:00:00.000Z`)` (o `new Date(entry.date)`, que en formato YYYY-MM-DD ya se interpreta como UTC-midnight). Esto es CRÍTICO: debe coincidir con la clave UTC-midnight que el gate de compliance construye en 07-02 al consultar `prisma.holiday.findFirst({ where: { date } })`. Hacer el upsert idempotente por la columna única `date`: `await prisma.holiday.upsert({ where: { date }, update: { name }, create: { date, name } })` dentro de un loop sobre `HOLIDAYS_CO`. Contar y loguear cuántos se procesaron (p. ej. `console.info(`Festivos CO cargados: ${HOLIDAYS_CO.length}`)`). Cerrar con el mismo bloque `main().catch(...).finally(() => prisma.$disconnect())` de backfill-consents.ts.

    En `packages/db/package.json`, agregar a "scripts" la entrada `"db:seed:holidays": "tsx src/seed-holidays.ts"`, ubicada junto a los otros `db:seed:*` / `db:backfill-*`, respetando el formato JSON existente. No usar bloques de código fenced en los archivos; escribir el TS y el JSON directamente.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/db typecheck && grep -q '"db:seed:holidays": "tsx src/seed-holidays.ts"' packages/db/package.json && node -e "const s=require('fs').readFileSync('packages/db/src/seed-holidays.ts','utf8');const m=s.match(/\d{4}-\d{2}-\d{2}/g)||[];if(m.length<36){console.error('faltan fechas: '+m.length);process.exit(1)}console.log('fechas OK: '+m.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/seed-holidays.ts` existe, importa `PrismaClient` y `loadSeedEnv`, y usa `prisma.holiday.upsert` con `where: { date }` (idempotente por la fecha única).
    - El array `HOLIDAYS_CO` contiene 36 entradas: 18 fechas de 2026 y 18 de 2027 (grep de fechas YYYY-MM-DD cuenta ≥ 36).
    - Las fechas se construyen a UTC-midnight (formato `YYYY-MM-DD` o sufijo `T00:00:00.000Z`), NO con hora local, para coincidir con la clave que 07-02 consulta.
    - `packages/db/package.json` contiene `"db:seed:holidays": "tsx src/seed-holidays.ts"`.
    - `pnpm --filter @cobrai/db typecheck` pasa.
    - Idempotencia: correr `pnpm --filter @cobrai/db db:seed:holidays` dos veces deja exactamente 36 filas en `holidays` (el 2º run no crea duplicados). Si la BD no está disponible en el entorno de ejecución, dejar registrado en el SUMMARY que la idempotencia se garantiza por el `upsert` sobre la columna única `date` y por el índice `@unique`.
  </acceptance_criteria>
  <done>El script `seed-holidays.ts` upserta los 36 festivos CO (2026+2027) de forma idempotente y el script npm `db:seed:holidays` existe; typecheck de @cobrai/db pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script seed → BD Postgres | El seed escribe filas en la tabla `holidays`; los datos son una lista curada estática, no entrada externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | seed-holidays HOLIDAYS_CO | accept | La lista de festivos es una constante hardcoded en el repo (no entrada externa); su corrección se verifica contra el calendario oficial CO en la acceptance (36 filas, 18/año). |
| T-07-02 | Denial of Service | prisma.holiday.upsert loop | accept | Loop acotado a 36 upserts sobre una tabla global mínima; ejecución manual puntual (anual), sin exposición a red. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes nuevos: `tsx`, `@prisma/client` y `prisma` ya están en devDependencies/dependencies de @cobrai/db. Sin superficie de supply-chain en este plan. |
</threat_model>

<verification>
- `grep 'model Holiday' packages/db/prisma/schema.prisma` encuentra el modelo; `prisma.holiday` disponible en el cliente generado.
- `pnpm --filter @cobrai/db typecheck` pasa.
- `packages/db/package.json` contiene `db:seed:holidays`.
- El array del seed tiene 36 fechas (18 de 2026 + 18 de 2027).
</verification>

<success_criteria>
- Modelo `Holiday` mínimo (id, date @unique @db.Date, name, createdAt) mapeado a `holidays`, sin countryCode ni tenantId.
- Tabla `holidays` aplicada a la BD y `prisma.holiday` disponible en el cliente (habilita 07-02).
- Seed idempotente `seed-holidays.ts` con los 36 festivos CO (2026+2027) upsert por fecha única + script `db:seed:holidays`.
</success_criteria>

<output>
Create `.planning/phases/07-d-as-festivos-colombia/07-01-SUMMARY.md` when done
</output>
