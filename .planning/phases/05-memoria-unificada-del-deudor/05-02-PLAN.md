---
phase: 05-memoria-unificada-del-deudor
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/service-notifications/src/agent/conversation-agent.service.ts
  - apps/service-notifications/src/agent/conversation-agent.service.spec.ts
  - apps/service-notifications/src/agent/agent.module.ts
autonomous: true
requirements: []   # No REQUIREMENTS.md IDs map to Phase 5. Tied to ROADMAP goal: WhatsApp agent reads unified cross-channel context.
user_setup: []

must_haves:
  truths:
    - "The WhatsApp agent (processInboundMessage) builds its system prompt from DebtorMemoryService.getUnifiedContext instead of the local loadDebtorHistory"
    - "The WhatsApp agent still responds, saves the outbound message, and applies intents exactly as before (no behavioral regression)"
    - "AgentModule imports MemoryModule so DebtorMemoryService is injectable into ConversationAgentService"
  artifacts:
    - path: "apps/service-notifications/src/agent/conversation-agent.service.ts"
      provides: "ConversationAgentService consuming getUnifiedContext for prompt context"
      contains: "getUnifiedContext"
    - path: "apps/service-notifications/src/agent/agent.module.ts"
      provides: "AgentModule importing MemoryModule"
      contains: "MemoryModule"
  key_links:
    - from: "apps/service-notifications/src/agent/conversation-agent.service.ts"
      to: "DebtorMemoryService.getUnifiedContext"
      via: "constructor-injected debtorMemory call in processInboundMessage"
      pattern: "debtorMemory\\.getUnifiedContext"
    - from: "apps/service-notifications/src/agent/agent.module.ts"
      to: "MemoryModule"
      via: "imports array"
      pattern: "MemoryModule"
---

<objective>
Integrate the unified memory into the WhatsApp conversational agent: replace the private `loadDebtorHistory` context source in `ConversationAgentService.processInboundMessage` with `DebtorMemoryService.getUnifiedContext`, and wire `MemoryModule` into `AgentModule`.

Purpose: The WhatsApp agent gains cross-channel context (voice summaries, living debtor profile) without changing how it responds. This is the first consumer of the Wave 1 service.

Output: Modified `conversation-agent.service.ts` (+ its spec) and `agent.module.ts`.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-memoria-unificada-del-deudor/05-CONTEXT.md
@.planning/phases/05-memoria-unificada-del-deudor/05-RESEARCH.md
@.planning/phases/05-memoria-unificada-del-deudor/05-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 — consume directly, do NOT re-derive. -->

From src/memory/debtor-memory.service.ts (Plan 01):
- `getUnifiedContext(tenantId: string, debtorId: string, debtId?: string): Promise<UnifiedDebtorContext>`
- `UnifiedDebtorContext = { debtorHistory: DebtorHistory(extended), emotionalProfile: EmotionalProfile | null }`
- `DebtorMemoryService` is exported by `MemoryModule` (src/memory/memory.module.ts).

CURRENT call site to change (conversation-agent.service.ts lines 109-122):
- line 110: `const debtorHistory = await this.loadDebtorHistory(debtor_id, tenant_id, debt.id);`
- lines 113-122: `const systemPrompt = buildSystemPrompt({ debtorName, companyName, amount, currency, dueDate, paymentLink, debtStatus, debtorHistory });`

CURRENT constructor (lines 46-51): `(private readonly config: ConfigService, private readonly prisma: PrismaService, private readonly kafka: KafkaService, private readonly whatsapp: TwilioWhatsAppAdapter)`.

CURRENT AgentModule (src/agent/agent.module.ts): `imports: [KafkaModule, AdaptersModule, ConfigModule]`, providers/exports [ConversationAgentService].

SPEC mock pattern to extend (conversation-agent.service.spec.ts lines 111-117): service is built with `new ConversationAgentService(makeConfig(), mockPrisma as never, mockKafka as never, mockWhatsapp as never)`. Landmine 4: add a `mockDebtorMemory` with `getUnifiedContext: vi.fn().mockResolvedValue({ debtorHistory: {...all 11 fields...}, emotionalProfile: null })` and `refreshMemory: vi.fn()`, passed as the new constructor arg.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject DebtorMemoryService and replace loadDebtorHistory with getUnifiedContext</name>
  <read_first>
    - apps/service-notifications/src/agent/conversation-agent.service.ts (constructor lines 46-61; processInboundMessage lines 63-195; loadDebtorHistory lines 282-344)
    - apps/service-notifications/src/memory/debtor-memory.service.ts (getUnifiedContext signature + UnifiedDebtorContext shape from Plan 01)
    - .planning/phases/05-memoria-unificada-del-deudor/05-RESEARCH.md (Topic 6 §1 — exact replacement; Landmine 4 — spec must mock DebtorMemoryService)
  </read_first>
  <action>
    Add `DebtorMemoryService` (imported from "../memory/debtor-memory.service") as the FIFTH constructor parameter of `ConversationAgentService`: `private readonly debtorMemory: DebtorMemoryService`. Keep the existing four params in order.
    In `processInboundMessage`, replace line 110 (`const debtorHistory = await this.loadDebtorHistory(...)`) with `const unifiedContext = await this.debtorMemory.getUnifiedContext(tenant_id, debtor_id, debt.id);` and pass `debtorHistory: unifiedContext.debtorHistory` into the `buildSystemPrompt({...})` call (replace the `debtorHistory` value only; leave all other prompt fields unchanged).
    Remove the now-unused private `loadDebtorHistory` method (lines 282-344) since its logic now lives in DebtorMemoryService.gatherRawHistory. Do NOT remove `extractText` (still used by the message-history mapping at line 131). Leave the message-history `findMany` (lines 102-107) and all intent/response logic untouched — only the context SOURCE changes.
  </action>
  <verify>
    <automated>cd apps/service-notifications && npx vitest run src/agent/conversation-agent.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "debtorMemory.getUnifiedContext" apps/service-notifications/src/agent/conversation-agent.service.ts` returns 1 match in processInboundMessage.
    - `grep -c "loadDebtorHistory" apps/service-notifications/src/agent/conversation-agent.service.ts` equals 0 (method removed, no dangling calls).
    - `grep -n "private readonly debtorMemory: DebtorMemoryService" apps/service-notifications/src/agent/conversation-agent.service.ts` confirms the injected dependency.
    - `grep -n "extractText" apps/service-notifications/src/agent/conversation-agent.service.ts` still present (not deleted).
    - `cd apps/service-notifications && npx tsc --noEmit` — no type errors.
  </acceptance_criteria>
  <done>processInboundMessage sources its prompt context from getUnifiedContext, loadDebtorHistory is gone, all other behavior intact, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the agent spec mock and wire MemoryModule into AgentModule</name>
  <read_first>
    - apps/service-notifications/src/agent/conversation-agent.service.spec.ts (full file — mockPrisma object, makeConfig, beforeEach service construction lines 105-117, all existing assertions)
    - apps/service-notifications/src/agent/agent.module.ts (imports array to extend)
    - .planning/phases/05-memoria-unificada-del-deudor/05-RESEARCH.md (Landmine 4 — exact mockDebtorMemory shape)
  </read_first>
  <action>
    In conversation-agent.service.spec.ts, add a `const mockDebtorMemory = { getUnifiedContext: vi.fn(), refreshMemory: vi.fn().mockResolvedValue(undefined) }` near the other mocks. In `beforeEach`, set `mockDebtorMemory.getUnifiedContext.mockResolvedValue({ debtorHistory: { previousContactsCount: 0, brokenPromisesCount: 0, lastOutcome: null, lastContactDaysAgo: null, preferredChannel: null, callSummary: null, hasPromisePending: false, promisedDate: null, livingSummary: null, overallSentiment: null, paymentBehavior: null }, emotionalProfile: null })` and pass it as the 5th arg to `new ConversationAgentService(makeConfig(), mockPrisma as never, mockKafka as never, mockWhatsapp as never, mockDebtorMemory as never)`.
    Add one new assertion test: "usa getUnifiedContext para construir el prompt" — after processInboundMessage, expect `mockDebtorMemory.getUnifiedContext` toHaveBeenCalledWith("org1", "debtor1", "debt1"). Keep ALL existing tests passing unchanged (they should still pass because the context source is mocked).
    In agent.module.ts, add `MemoryModule` (imported from "../memory/memory.module") to the `imports` array alongside KafkaModule, AdaptersModule, ConfigModule.
  </action>
  <verify>
    <automated>cd apps/service-notifications && npx vitest run src/agent/conversation-agent.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mockDebtorMemory" apps/service-notifications/src/agent/conversation-agent.service.spec.ts` shows the mock defined AND passed to the constructor.
    - New test asserts `getUnifiedContext` called with `("org1", "debtor1", "debt1")`.
    - All pre-existing agent spec tests still pass (intent unrelated/promise/dispute/escalate/opt_out, OpenAI-throws-fallback, no-debt, debtor-not-found, history-context) — full file green.
    - `grep -n "MemoryModule" apps/service-notifications/src/agent/agent.module.ts` confirms the import is wired.
    - `cd apps/service-notifications && npx tsc --noEmit` — no type errors.
  </acceptance_criteria>
  <done>Agent spec injects the mocked DebtorMemoryService, the new getUnifiedContext assertion passes, all prior tests stay green, AgentModule imports MemoryModule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound WhatsApp payload → agent | debtor_id/tenant_id from upstream Kafka event; already scoped by existing queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | unified context replacing loadDebtorHistory | mitigate | getUnifiedContext is read-only and tenant-scoped (Plan 01 T-05-02); WhatsApp behavior otherwise unchanged |
| T-05-05 | Denial of Service | getUnifiedContext failure blocks inbound reply | accept | Single read aggregation; if it throws the message handler errors as before any external call — no new failure surface beyond existing prisma reads. (refreshMemory is NOT called here; that hook is Plan 04 and is try/catch wrapped.) |
| T-05-SC | Tampering | npm installs | accept | No new packages this phase |
</threat_model>

<verification>
- `cd apps/service-notifications && npx vitest run src/agent/conversation-agent.service.spec.ts` — all green including the new getUnifiedContext assertion.
- `cd apps/service-notifications && npx tsc --noEmit` — clean.
- `grep -rn "MemoryModule" apps/service-notifications/src/agent/agent.module.ts` — wired.
</verification>

<success_criteria>
- WhatsApp agent builds its prompt from getUnifiedContext; loadDebtorHistory removed.
- No regression: all existing agent spec tests pass.
- AgentModule imports MemoryModule; DI resolves DebtorMemoryService.
</success_criteria>

<output>
Create `.planning/phases/05-memoria-unificada-del-deudor/05-02-SUMMARY.md` when done.
</output>
