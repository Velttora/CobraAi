---
phase: 05-memoria-unificada-del-deudor
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/service-notifications/src/contacts/contacts.service.ts
  - apps/service-notifications/src/contacts/contacts.service.spec.ts
  - apps/service-notifications/src/contacts/contacts.module.ts
autonomous: true
requirements: []   # No REQUIREMENTS.md IDs map to Phase 5. Tied to ROADMAP goal: voice agent stops being blind to other channels.
user_setup: []

must_haves:
  truths:
    - "loadVoiceCallHistory enriches the Vapi strategy_context.variables with the unified debtor profile (perfil_deudor, sentimiento_previo, comportamiento_pago) from getUnifiedContext"
    - "The voice call flow still produces the existing variable keys (es_seguimiento, contactos_previos, dias_ultimo_contacto, tiene_promesa_pendiente, promesas_rotas, first_message_override) with no regression"
    - "ContactsModule imports MemoryModule so DebtorMemoryService is injectable into ContactsService"
  artifacts:
    - path: "apps/service-notifications/src/contacts/contacts.service.ts"
      provides: "loadVoiceCallHistory enriched via getUnifiedContext"
      contains: "getUnifiedContext"
    - path: "apps/service-notifications/src/contacts/contacts.service.spec.ts"
      provides: "NEW vitest spec covering loadVoiceCallHistory enrichment (no spec existed before)"
    - path: "apps/service-notifications/src/contacts/contacts.module.ts"
      provides: "ContactsModule importing MemoryModule"
      contains: "MemoryModule"
  key_links:
    - from: "apps/service-notifications/src/contacts/contacts.service.ts"
      to: "DebtorMemoryService.getUnifiedContext"
      via: "constructor-injected debtorMemory call in loadVoiceCallHistory"
      pattern: "debtorMemory\\.getUnifiedContext"
    - from: "apps/service-notifications/src/contacts/contacts.module.ts"
      to: "MemoryModule"
      via: "imports array"
      pattern: "MemoryModule"
---

<objective>
Make the voice agent no longer blind: enrich `ContactsService.loadVoiceCallHistory` (the Vapi `strategy_context.variables` builder) with the unified debtor profile from `DebtorMemoryService.getUnifiedContext`, and wire `MemoryModule` into `ContactsModule`.

Purpose: Today the voice path only reads contact counts. After this, a voice call can reference what the debtor said on WhatsApp (via the living summary), closing the asymmetry called out in CONTEXT.md.

Output: Modified `contacts.service.ts`, a NEW `contacts.service.spec.ts` (none exists — required by the Nyquist rule), and `contacts.module.ts`.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-memoria-unificada-del-deudor/05-CONTEXT.md
@.planning/phases/05-memoria-unificada-del-deudor/05-RESEARCH.md
@.planning/phases/05-memoria-unificada-del-deudor/05-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 — consume directly. -->

From src/memory/debtor-memory.service.ts (Plan 01):
- `getUnifiedContext(tenantId, debtorId, debtId?): Promise<UnifiedDebtorContext>`
- `UnifiedDebtorContext.emotionalProfile: EmotionalProfile | null` where EmotionalProfile has `summary`, `sentiment`, `paymentBehavior`.
- `DebtorMemoryService` exported by `MemoryModule`.

CURRENT loadVoiceCallHistory (contacts.service.ts lines 370-421) returns this Record<string,string> (keys consumed by Vapi variable substitution):
- es_seguimiento, contactos_previos, dias_ultimo_contacto, tiene_promesa_pendiente, promesas_rotas, first_message_override.
- It already queries: contacts (findMany completed take 10), pendingPromise (findFirst), brokenCount (count). It computes firstMessage via 4 branches (count===0 / pendingPromise / brokenCount>0 / else).
- Called from dispatchChannel case "voice" (line 296): `await this.loadVoiceCallHistory(debtor.id, tenantId, debt.id, debtor.name, String(decimalToNumber(debt.amountOutstanding)), new Date(debt.dueDate).toISOString())`. The returned record is spread into `strategy_context.variables` (line 307: `variables: { ...variables, ...callHistory }`).

CURRENT ContactsService constructor (lines 45-55), 9 params in order:
`(prisma, compliance, email, sms, whatsapp, voice, kafka, waterfall, config)`.

CURRENT ContactsModule (src/contacts/contacts.module.ts): `imports: [ComplianceModule, AdaptersModule, OrchestratorModule, KafkaModule, AgentModule]`, providers [ContactsService, KafkaConsumerService], exports [ContactsService].

NO contacts.service.spec.ts exists yet — create one. There is no NestJS testing module precedent in this app; instantiate the service directly with `new ContactsService(...mocks)` matching the constructor order, mirroring conversation-agent.service.spec.ts conventions (plain `vi.fn()` mocks cast `as never`/`as any`). loadVoiceCallHistory is PRIVATE — exercise it indirectly through the public `executeContact(tenantId, { debt_id, channel: "voice" })` path (which calls dispatchChannel → loadVoiceCallHistory → voice.initiateCall), then assert on the `voice.initiateCall` mock's `strategy_context.variables`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject DebtorMemoryService and enrich loadVoiceCallHistory with the unified profile</name>
  <read_first>
    - apps/service-notifications/src/contacts/contacts.service.ts (constructor lines 45-55; dispatchChannel "voice" case lines 293-315; loadVoiceCallHistory lines 370-421)
    - apps/service-notifications/src/memory/debtor-memory.service.ts (getUnifiedContext + EmotionalProfile fields from Plan 01)
    - .planning/phases/05-memoria-unificada-del-deudor/05-RESEARCH.md (Topic 6 §2 — exact enrichment keys perfil_deudor / sentimiento_previo / comportamiento_pago)
  </read_first>
  <action>
    Add `DebtorMemoryService` (imported from "../memory/debtor-memory.service") as the TENTH constructor parameter of `ContactsService`: `private readonly debtorMemory: DebtorMemoryService` after `config`. Keep the existing nine params in their exact order.
    In `loadVoiceCallHistory`, near the top call `const ctx = await this.debtorMemory.getUnifiedContext(tenantId, debtorId);` (debtId omitted — voice history is debtor-level). Keep ALL existing logic (contacts/pendingPromise/brokenCount queries, the 4-branch firstMessage, the existing returned keys). Extend the returned record with three NEW keys sourced from `ctx.emotionalProfile`: `perfil_deudor: ctx.emotionalProfile?.summary ?? ""`, `sentimiento_previo: ctx.emotionalProfile?.sentiment ?? "neutral"`, `comportamiento_pago: ctx.emotionalProfile?.paymentBehavior ?? "desconocido"`. Do not remove or rename existing keys. Do not change dispatchChannel's call signature.
  </action>
  <verify>
    <automated>cd apps/service-notifications && npx vitest run src/contacts/contacts.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "debtorMemory.getUnifiedContext" apps/service-notifications/src/contacts/contacts.service.ts` returns 1 match inside loadVoiceCallHistory.
    - `grep -c "perfil_deudor\|sentimiento_previo\|comportamiento_pago" apps/service-notifications/src/contacts/contacts.service.ts` is 3 (the new keys present).
    - `grep -c "es_seguimiento\|contactos_previos\|dias_ultimo_contacto\|tiene_promesa_pendiente\|promesas_rotas\|first_message_override" apps/service-notifications/src/contacts/contacts.service.ts` confirms all 6 original keys retained.
    - `grep -n "private readonly debtorMemory: DebtorMemoryService" apps/service-notifications/src/contacts/contacts.service.ts` confirms injection.
    - `cd apps/service-notifications && npx tsc --noEmit` — no type errors.
  </acceptance_criteria>
  <done>loadVoiceCallHistory calls getUnifiedContext and adds perfil_deudor/sentimiento_previo/comportamiento_pago to the Vapi variables while retaining all existing keys; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create contacts.service.spec.ts for voice enrichment + wire MemoryModule into ContactsModule</name>
  <read_first>
    - apps/service-notifications/src/contacts/contacts.service.ts (executeContact lines 115-236; dispatchChannel "voice" lines 293-315 — the path the test drives; getDebtContext lines 496-505; resolveMessageChannel lines 239-246; buildVariables lines 423-444)
    - apps/service-notifications/src/agent/conversation-agent.service.spec.ts (mock conventions: plain vi.fn() objects, makeConfig, beforeEach + vi.clearAllMocks, cast as never)
    - apps/service-notifications/src/contacts/contacts.module.ts (imports array to extend)
  </read_first>
  <action>
    Create apps/service-notifications/src/contacts/contacts.service.spec.ts. Build `new ContactsService(...)` with plain `vi.fn()` mocks in the constructor's 10-arg order: prisma, compliance, email, sms, whatsapp, voice, kafka, waterfall, config, debtorMemory.
    Mock essentials so the voice path runs through executeContact: `compliance.checkBeforeSend` → `{ allowed: true }`; `config.get` → return "true" for FEATURE_SMS_ENABLED only if needed (voice channel is unaffected by SMS gating, so a default null is fine); prisma.debt.findFirst (getDebtContext) → a debt with nested debtor (id, name, phones:["+573001234567"], email); prisma.contact.create/update, prisma.conversation.findFirst/create, prisma.message.create, prisma.promiseToPay.findFirst/count, prisma.notificationTemplate.findFirst (resolveTemplate) → null is acceptable; `voice.initiateCall` → `{ call_id: "call1", status: "queued" }`; `kafka.publish` → resolved. Provide `debtorMemory.getUnifiedContext = vi.fn().mockResolvedValue({ debtorHistory: {...neutral 11 fields...}, emotionalProfile: { summary: "Deudor cooperativo, prometió pagar.", sentiment: "positivo", lastIntent: "promesa_pago", paymentBehavior: "cumplidor", sentimentScore: 0.7, updatedAt: new Date().toISOString(), interactionCount: 2 } })`.
    Write at least 3 tests:
      1. "voice call inyecta perfil_deudor en strategy_context.variables" — call `await service.executeContact("org1", { debt_id: "debt1", channel: "voice" })`; assert `voice.initiateCall` called with `strategy_context.variables` objectContaining `{ perfil_deudor: "Deudor cooperativo, prometió pagar.", sentimiento_previo: "positivo", comportamiento_pago: "cumplidor" }`.
      2. "voice call con emotionalProfile null usa defaults" — override `getUnifiedContext` to resolve `emotionalProfile: null`; assert variables objectContaining `{ perfil_deudor: "", sentimiento_previo: "neutral", comportamiento_pago: "desconocido" }`.
      3. "voice call conserva las llaves existentes" — assert variables objectContaining `es_seguimiento`, `contactos_previos`, `first_message_override` keys are present (defined).
    In contacts.module.ts, add `MemoryModule` (imported from "../memory/memory.module") to the `imports` array.
  </action>
  <verify>
    <automated>cd apps/service-notifications && npx vitest run src/contacts/contacts.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File apps/service-notifications/src/contacts/contacts.service.spec.ts exists and `npx vitest run src/contacts/contacts.service.spec.ts` passes all 3 tests.
    - Test 1 asserts `voice.initiateCall` strategy_context.variables contains perfil_deudor/sentimiento_previo/comportamiento_pago from the mocked profile.
    - Test 2 asserts the null-profile defaults ("" / "neutral" / "desconocido").
    - `grep -n "MemoryModule" apps/service-notifications/src/contacts/contacts.module.ts` confirms the import is wired.
    - `cd apps/service-notifications && npx tsc --noEmit` — no type errors.
  </acceptance_criteria>
  <done>New contacts spec drives the voice path through executeContact and proves the unified-profile keys reach Vapi variables (incl. null-profile defaults); ContactsModule imports MemoryModule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → Vapi strategy_context | the living summary text is sent to the Vapi agent prompt; it originates from LLM output already sanitized in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | profile summary injected into voice variables | mitigate | getUnifiedContext is tenant-scoped (Plan 01 T-05-02); only the debtor's own summary is read |
| T-05-07 | Denial of Service | getUnifiedContext failure blocks voice dispatch | accept | A single read; failure surfaces like any existing prisma read in dispatchChannel. No new external dependency added to the hot path beyond Plan 01's read |
| T-05-SC | Tampering | npm installs | accept | No new packages this phase |
</threat_model>

<verification>
- `cd apps/service-notifications && npx vitest run src/contacts/contacts.service.spec.ts` — all green.
- `cd apps/service-notifications && npx tsc --noEmit` — clean.
- `grep -rn "MemoryModule" apps/service-notifications/src/contacts/contacts.module.ts` — wired.
</verification>

<success_criteria>
- loadVoiceCallHistory enriches Vapi variables with perfil_deudor/sentimiento_previo/comportamiento_pago.
- All existing voice variable keys retained (no regression).
- New contacts spec exists and passes; ContactsModule imports MemoryModule.
</success_criteria>

<output>
Create `.planning/phases/05-memoria-unificada-del-deudor/05-03-SUMMARY.md` when done.
</output>
