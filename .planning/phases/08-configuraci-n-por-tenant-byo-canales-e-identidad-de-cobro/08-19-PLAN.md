---
phase: 08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro
plan: 19
type: execute
wave: 7
depends_on: ["08-15", "08-16"]
files_modified:
  - apps/web/components/settings/integrations/BrandIdentityPanel.tsx
  - apps/web/components/settings/integrations/BrandIdentityPanel.test.tsx
  - apps/web/components/settings/integrations/BrandMessagePreview.tsx
  - apps/web/components/settings/integrations/BrandMessagePreview.test.tsx
  - apps/web/components/settings/email-builder/SignatureEditor.tsx
  - apps/web/components/settings/integrations/IntegrationHealthPanel.tsx
  - apps/web/components/settings/integrations/UncontactedDebtsTable.tsx
  - apps/web/components/settings/integrations/UncontactedDebtsTable.test.tsx
  - apps/web/app/(dashboard)/settings/integrations/brand/page.tsx
  - apps/web/app/(dashboard)/settings/integrations/health/page.tsx
  - apps/web/hooks/use-tenant.ts
autonomous: true
requirements: [D-16, D-24]

must_haves:
  truths:
    - "A tenant edits its commercial identity in one place and sees, live, how the message reaches the debtor on WhatsApp, email and a call"
    - "The email builder no longer lets company identity be edited in a second place that could diverge"
    - "A tenant can see which integrations are operational and which failed"
    - "A tenant can see exactly which debts stopped because a channel is not configured, and what to connect to resume them"
  artifacts:
    - path: "apps/web/components/settings/integrations/BrandMessagePreview.tsx"
      provides: "Three-pane WhatsApp/email/voice preview bound to the live draft"
      min_lines: 90
    - path: "apps/web/components/settings/integrations/UncontactedDebtsTable.tsx"
      provides: "The channel_not_configured debt list with its four states"
  key_links:
    - from: "apps/web/components/settings/integrations/BrandMessagePreview.tsx"
      to: "components/settings/email-builder/LayoutPreview.tsx"
      via: "the email pane reuses the real renderer, unchanged"
      pattern: "LayoutPreview"
    - from: "apps/web/components/settings/email-builder/SignatureEditor.tsx"
      to: "/settings/integrations/brand"
      via: "company identity fields become read-only mirrors with a link to the canonical editor"
      pattern: "/settings/integrations/brand"
---

<objective>
Build the last two screens: `Identidad de marca` — the company identity form with a live three-pane preview of what the debtor actually receives — and `Estado y salud` — the per-integration operational status plus the list of debts stopped because a channel is not configured.

Purpose: D-24 screens 3 and 4. The brand screen also closes UI-SPEC assumption A-05: `SignatureEditor` currently edits the same company fields in the email builder, and two editable copies of a company name would produce different names in email and WhatsApp — exactly the failure this phase exists to fix. The health screen is the payoff for D-16: it is where a tenant discovers that debts stopped moving and why.
Output: `BrandIdentityPanel`, `BrandMessagePreview`, the `SignatureEditor` read-through, `IntegrationHealthPanel`, `UncontactedDebtsTable` and both pages.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-UI-SPEC.md
@.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-16-SUMMARY.md
@.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-15-SUMMARY.md
@.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-14-SUMMARY.md

shadcn is NOT installed. Hand-rolled Tailwind only. Component tests are `*.test.tsx` — the vitest include glob does not match `.spec.tsx`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BrandIdentityPanel and the SignatureEditor read-through</name>
  <files>apps/web/components/settings/integrations/BrandIdentityPanel.tsx, apps/web/components/settings/integrations/BrandIdentityPanel.test.tsx, apps/web/components/settings/email-builder/SignatureEditor.tsx, apps/web/hooks/use-tenant.ts, apps/web/app/(dashboard)/settings/integrations/brand/page.tsx</files>
  <read_first>
    - apps/web/components/settings/email-builder/SignatureEditor.tsx (whole file — the field layout to mirror, and specifically which fields become read-only: `companyName`, `logoUrl`, `address`, `phone`, `website`, `legalDisclaimer`; `socials` stays editable here)
    - apps/web/components/settings/ContactRetryPolicyPanel.tsx (the fieldset/grid layout, the `min-h-16` textarea, the `isDirty` computation and the admin/read-only fork)
    - apps/web/components/settings/OrganizationSettingsPanel.tsx (the panel shell with its `Building2` icon and accent submit button)
    - apps/web/hooks/use-tenant.ts (whole file — the hook pair pattern; a `useUpdateBrandIdentity` mutation is added here following `useUpdateContactRetryPolicy`)
    - packages/utils/src/brand-identity.ts (from 08-15 — the nine-field `BrandIdentity` shape and `EMPRESA_FALLBACK`)
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-15-SUMMARY.md (the endpoint, the field list and the `empresa_*` variable names)
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-UI-SPEC.md "Screen 3 — Identidad de marca" (the three fieldset groups, the fallback hint copy, the two-column `lg:grid-cols-5` layout, and the Screen 3 copy matrix) and assumption A-05
  </read_first>
  <behavior>
    - The panel renders three `<fieldset>` groups — `Identidad`, `Contacto`, `Firma legal` — with the fields UI-SPEC lists in each
    - Under `Nombre comercial` the hint reads `Es el nombre que ve el deudor en cada mensaje. Si lo dejas vacío, aparecerá "su gestor de cobranza".`
    - `Nombre comercial` is marked required with a visible marker and `aria-required`
    - Saving calls the brand-identity mutation and shows `Identidad de marca actualizada` on success and `No se pudo guardar la identidad de marca` on failure
    - The submit button is disabled unless the form is dirty
    - A non-admin sees the read-only `<dl>` view and the not-an-admin notice
    - The empty state shows `Tus mensajes salen sin marca` with its body copy
    - In `SignatureEditor`, the six company-identity fields render as read-only values with the link `Estos datos se editan en Integraciones → Marca` pointing at `/settings/integrations/brand`
    - In `SignatureEditor`, `socials` remains fully editable
    - The brand page lays out the form and the preview as a `lg:grid-cols-5` split with the preview sticky, stacking below `lg`
  </behavior>
  <action>
    Add `useUpdateBrandIdentity` to `apps/web/hooks/use-tenant.ts`, following `useUpdateContactRetryPolicy` exactly: `patchApi` against `/api/v1/tenant/brand-identity`, invalidating `["tenant"]` on success. It belongs here rather than in `use-integrations.ts` because brand identity is part of the tenant profile, not a `TenantIntegration`.

    Create `BrandIdentityPanel` using the canonical panel shell with the `Building2` icon, the three fieldsets from UI-SPEC, and the existing admin-fork and dirty-check patterns. Contact and legal fields go two-up at `md` (`md:grid-cols-2`), matching the grid idiom already in `SignatureEditor`. The address and legal-notice textareas use `min-h-16`, the declared exception already present in the codebase.

    Edit `SignatureEditor` to stop being a second editor of company identity. The six overlapping fields become read-only renderings of the tenant's brand identity, sourced from `useTenant()`, with an inline link to `/settings/integrations/brand`. Leave `socials` editable and leave every other behaviour of the email builder untouched — this is a targeted change, not a rewrite. Add an English comment recording UI-SPEC A-05: two editable copies of the company name would silently diverge and produce different names in email versus WhatsApp.

    Assemble `brand/page.tsx` as `<div className="grid gap-6 lg:grid-cols-5">` with the panel at `lg:col-span-3` and the preview at `lg:col-span-2 lg:sticky lg:top-6`, per UI-SPEC.

    Only spacing tokens and typography sizes from UI-SPEC's scale tables. Write `BrandIdentityPanel.test.tsx` covering every bullet, mocking the hooks and `useAuth`.

    All identifiers and comments in English; all user-visible copy in Spanish, verbatim from UI-SPEC.
  </action>
  <verify>
    <automated>pnpm --filter @cobrai/web test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @cobrai/web test` passes with a test for every bullet in the behavior block
    - `grep -n "/settings/integrations/brand" apps/web/components/settings/email-builder/SignatureEditor.tsx` returns a match
    - A test asserts the six company-identity fields in `SignatureEditor` render no editable input, while `socials` still does
    - A test asserts the fallback hint copy renders under `Nombre comercial`
    - A test asserts a non-admin sees no form
    - `grep -n "brand-identity" apps/web/hooks/use-tenant.ts` returns a match
    - `pnpm --filter @cobrai/web typecheck` exits 0
  </acceptance_criteria>
  <done>Company identity has one editable home, and the email builder mirrors it instead of forking it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BrandMessagePreview — three panes showing what the debtor actually receives</name>
  <files>apps/web/components/settings/integrations/BrandMessagePreview.tsx, apps/web/components/settings/integrations/BrandMessagePreview.test.tsx, apps/web/app/(dashboard)/settings/integrations/brand/page.tsx</files>
  <read_first>
    - apps/web/components/settings/email-builder/LayoutPreview.tsx (whole file — it already renders through the real `renderEmailLayout` in a sandboxed iframe; the email pane wraps it unchanged, per UI-SPEC "non-negotiable")
    - packages/utils/src/email-layout.ts (the `RenderEmailContext` `brand` field added in plan 08-15 — the preview must pass the live draft through it so preview and production render identically)
    - apps/web/hooks/use-debounce.ts (the 300ms debounce UI-SPEC specifies)
    - apps/web/app/(dashboard)/conversations/page.tsx lines 185-203 (the pill control the pane switcher is styled on, upgraded here to real tab semantics because it does not navigate)
    - packages/utils/src/brand-identity.ts (from 08-15 — `EMPRESA_FALLBACK` and `resolveBrandVariables`)
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-UI-SPEC.md "Screen 3 — Identidad de marca" right column (the three-pane table, the WhatsApp bubble styling, the voice opening line, the live-update and fallback rules) and "Accessibility Requirements" (the tablist contract for a non-navigating switcher)
  </read_first>
  <behavior>
    - Three panes render — WhatsApp (default), Correo and Voz — switched by a segmented control with `role="tablist"`, `role="tab"`, `aria-selected` and arrow-key navigation
    - The WhatsApp pane renders a chat-bubble mock containing a real template body with `empresa` bound to the live draft's commercial name
    - The Correo pane renders through `LayoutPreview` unchanged, with the draft brand values injected into the layout's signature configuration
    - The Voz pane renders the agent's opening line as text with the draft company name interpolated
    - All three panes update from the draft, debounced at 300ms, with no manual refresh button
    - When `Nombre comercial` is empty, every pane shows the real fallback `su gestor de cobranza` in `text-slate-500 italic`, never a `{placeholder}` token
    - A visually hidden `aria-live="polite"` region announces `Vista previa actualizada` on change
    - The WhatsApp pane's header shows the connected number's display name, or `Sin conectar` in slate when no WhatsApp integration is verified
  </behavior>
  <action>
    Create `BrandMessagePreview` (props `{ draft, whatsappIntegration }`) implementing UI-SPEC's three-pane contract.

    The pane switcher does not navigate, so unlike `IntegrationsTabs` it uses real ARIA tab semantics: `role="tablist"`, `role="tab"`, `aria-selected`, `role="tabpanel"` and arrow-key navigation. UI-SPEC's accessibility table is explicit about the distinction between the two.

    The Correo pane must wrap `LayoutPreview` rather than reimplementing email rendering — UI-SPEC calls this non-negotiable, because the email preview already goes through the real renderer today and a second renderer would drift. Pass the draft brand values through the `brand` field that plan 08-15 added to `RenderEmailContext`.

    Fallback handling is the point of the screen: when `commercialName` is empty every pane shows `EMPRESA_FALLBACK` imported from `@cobrai/utils`, styled `text-slate-500 italic`. Never render a raw placeholder token — the tenant must see exactly what the debtor would see.

    Debounce the draft with the existing `use-debounce` hook at 300ms. Add the visually hidden `aria-live="polite"` summary region.

    Wire it into `brand/page.tsx`'s sticky right column, bound to the panel's live draft rather than to the saved value, so the tenant sees unsaved edits.

    Write `BrandMessagePreview.test.tsx` covering every bullet, mocking `LayoutPreview`. The fallback test must assert the literal `su gestor de cobranza` appears and that no `{` token does.

    All identifiers and comments in English; all user-visible copy in Spanish, verbatim from UI-SPEC.
  </action>
  <verify>
    <automated>pnpm --filter @cobrai/web test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @cobrai/web test` passes with a test for every bullet in the behavior block
    - `grep -n "LayoutPreview" apps/web/components/settings/integrations/BrandMessagePreview.tsx` returns a match
    - `grep -c "role=\"tablist\"\|role=\"tab\"\|aria-selected" apps/web/components/settings/integrations/BrandMessagePreview.tsx` is at least 3
    - A test asserts arrow-key navigation moves between panes
    - A test asserts the empty-name case renders `su gestor de cobranza` and no `{` placeholder token
    - A test asserts the `aria-live` region announces on change
    - `pnpm --filter @cobrai/web typecheck` exits 0
  </acceptance_criteria>
  <done>A tenant sees, live and in three channels, exactly how its brand reaches the debtor — including the fallback when it leaves the name empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IntegrationHealthPanel and UncontactedDebtsTable</name>
  <files>apps/web/components/settings/integrations/IntegrationHealthPanel.tsx, apps/web/components/settings/integrations/UncontactedDebtsTable.tsx, apps/web/components/settings/integrations/UncontactedDebtsTable.test.tsx, apps/web/app/(dashboard)/settings/integrations/health/page.tsx</files>
  <read_first>
    - apps/web/components/dashboard/AlertFeed.tsx (the list idiom and the centered `text-sm text-slate-500` empty state this panel and table follow — UI-SPEC A-12 rules out inventing an illustration system)
    - apps/web/components/shared/Skeleton.tsx (`TableSkeleton`, reused for the loading state)
    - apps/web/lib/contact-channels.ts (`channelLabel()` — reused for the blocked-channel column, adding nothing)
    - apps/web/lib/formatters.ts (the relative-date and currency helpers the table columns use)
    - apps/web/hooks/use-integrations.ts (from 08-16 — `useIntegrationHealth`, `useUncontactedDebts`)
    - apps/web/components/settings/integrations/IntegrationStatusBadge.tsx (from 08-16)
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-14-SUMMARY.md (the health and uncontacted-debts response shapes)
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-UI-SPEC.md "Screen 4 — Estado y salud" in full (the row-state table with its per-state detail text and action, the four-column table, the five table states with their exact copy, and the pagination rule) and "Responsive Behavior" (stacked cards below 640px)
  </read_first>
  <behavior>
    - `IntegrationHealthPanel` renders a `<ul>` with one row per integration — WhatsApp, Teléfono, Correo, Cobro — each showing icon, channel name, `IntegrationStatusBadge`, a detail line and an action
    - The panel header shows `{n} de {m} integraciones operativas`, counting only `verified` as operational
    - A `failed` row shows the provider message truncated to one line with the full text in `title`, and an accent `Arreglar` button deep-linking with the `?focus=` parameter
    - A `not_configured` row shows `Sin configurar — no se envía nada por este canal` and an accent `Configurar` button
    - `UncontactedDebtsTable` renders four columns — Deudor, Deuda, Canal bloqueado, Desde — with the debtor and debt cells linked to their detail routes
    - The loading state renders `TableSkeleton rows={5}`
    - Empty with every channel verified shows the teal `CheckCircle2` positive framing `Ninguna deuda detenida.` / `Todos los canales están operativos.`
    - Empty with channels missing shows the forward-looking copy plus a `Configurar canales` button
    - Non-empty renders the table plus a top-of-table accent callout `{n} deudas detenidas. Conecta {canal} para reanudarlas.`
    - The error state shows the load-error copy plus a `Reintentar` action
    - Pagination is 25 per page using the existing dashboard pagination idiom
    - Below 640px the table renders as stacked cards, one per debt
  </behavior>
  <action>
    Create `IntegrationHealthPanel` as the canonical panel shell containing a `<ul>` rather than a table — UI-SPEC specifies a list because it is at most five rows and must reflow on mobile. Implement the four row states from UI-SPEC's row-state table, with the deep links carrying the `?focus=` parameter contract recorded in 08-16's summary.

    Create `UncontactedDebtsTable` implementing all five states from UI-SPEC's state table, with copy taken verbatim. Reuse `channelLabel()` from `lib/contact-channels.ts` and the existing formatters — add no new helper for either. Below `sm`, render stacked cards instead of table rows, using Tailwind responsive classes on two sibling renderings, matching how `DnsRecordsTable` handles the same requirement in plan 08-17.

    Empty states are a centered `text-sm text-slate-500` line with an icon where a status is implied, following `AlertFeed`. Do not invent an illustration system (UI-SPEC A-12).

    The distinction between the two empty states matters and must be implemented, not collapsed: "no debts stopped and everything is connected" is good news, while "no debts stopped yet but nothing is connected" is a warning about what happens next.

    Assemble `health/page.tsx` as `<div className="space-y-6">` with the panel above the table.

    Write `UncontactedDebtsTable.test.tsx` covering every bullet, mocking the hooks.

    All identifiers and comments in English; all user-visible copy in Spanish, verbatim from UI-SPEC.
  </action>
  <verify>
    <automated>pnpm --filter @cobrai/web test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @cobrai/web test` passes with a test for every bullet in the behavior block
    - A test asserts the two empty states render different copy depending on whether channels are verified
    - A test asserts the non-empty state renders the accent callout naming the channel to connect
    - `grep -n "channelLabel" apps/web/components/settings/integrations/UncontactedDebtsTable.tsx` returns a match
    - `grep -n "TableSkeleton" apps/web/components/settings/integrations/UncontactedDebtsTable.tsx` returns a match
    - `grep -n "focus=" apps/web/components/settings/integrations/IntegrationHealthPanel.tsx` returns a match
    - `grep -cE "p-\[|mt-\[|gap-\[" apps/web/components/settings/integrations/IntegrationHealthPanel.tsx apps/web/components/settings/integrations/UncontactedDebtsTable.tsx` equals 0
    - `pnpm --filter @cobrai/web typecheck` exits 0
  </acceptance_criteria>
  <done>A tenant can see at a glance which integrations work, which failed, and exactly which debts are waiting on a channel that is not connected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tenant-authored brand fields → rendered preview | Tenant input is rendered into an email preview iframe |
| API response → rendered health rows | Provider error messages are rendered verbatim |
| Debtor data → settings screen | The uncontacted-debts table surfaces debtor names and amounts inside a settings page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | brand or provider text rendered as raw HTML | mitigate | All text renders as React children, which escapes by default; the email pane goes through `LayoutPreview`'s existing sandboxed iframe and the `escapeHtml` already inside `renderEmailLayout`. No `dangerouslySetInnerHTML` is introduced |
| T-08-19b | Spoofing | a preview that differs from what the debtor actually receives | mitigate | The email pane wraps the real `LayoutPreview`/`renderEmailLayout`, and the fallback uses the same `EMPRESA_FALLBACK` constant the adapters use (plan 08-15) |
| T-08-19c | Information Disclosure | debtor personal data exposed on a settings screen to a non-privileged member | accept | The uncontacted-debts list shows the same debtor names and amounts already visible on the debts and conversations screens to any authenticated member of the tenant; it introduces no new data class and is tenant-scoped server-side (plan 08-14) |
| T-08-19d | Information Disclosure | a third-party script reading brand fields from the preview iframe | mitigate | `LayoutPreview`'s iframe is sandboxed and unchanged; the Meta SDK is loaded only on the channels route (plan 08-17), never here |
| T-08-19e | Tampering | company identity edited in two places diverging silently | mitigate | `SignatureEditor`'s six overlapping fields become read-only mirrors with a link to the canonical editor; asserted by test |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] — not applicable: no package is installed in this plan |
</threat_model>

<verification>
- `pnpm --filter @cobrai/web test` green
- `pnpm --filter @cobrai/web typecheck` and `lint` exit 0
- `pnpm test` green at wave end
</verification>

<success_criteria>
- Brand identity is editable in one place with a live, real-renderer preview across three channels
- The email builder mirrors company identity instead of forking it
- Integration health is visible per channel with actionable deep links
- Debts blocked by `channel_not_configured` are listed with the channel to connect, and the two empty states are distinguished
- Nothing added to the design system
</success_criteria>

<output>
Create `.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-19-SUMMARY.md` when done. This is the last plan of the phase — also record any UI-SPEC item deliberately left unimplemented, so `/gsd-verify-work` can check it against the phase goal.
</output>
