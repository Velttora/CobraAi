---
phase: 08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md
autonomous: false
requirements: [D-02, D-03, D-05]

must_haves:
  truths:
    - "The exact Twilio Node SDK call chain for subaccount creation and WhatsApp sender registration is known, not guessed"
    - "The exact Vapi phone-number import request body and response field names are known"
    - "It is known whether SendGrid domain authentication for a subuser uses the parent key with On-Behalf-Of or a post-hoc association call"
    - "It is known whether the live SendGrid parent account is on a tier that permits subusers"
  artifacts:
    - path: ".planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md"
      provides: "Verified request/response contracts for Twilio subaccounts, Twilio Senders API, Vapi number import, SendGrid subusers"
      contains: "## Twilio"
  key_links:
    - from: "08-PROVIDER-CONTRACTS.md"
      to: "apps/service-notifications/node_modules/twilio type definitions"
      via: "recorded evidence of the resolved SDK method chain"
      pattern: "messaging"
---

<objective>
Discharge the three unverified provider assumptions that Wave 3's provisioning plans would otherwise code against blind: RESEARCH.md assumption A1 (Twilio Senders API SDK method name), A2 (Vapi number-import body/response), and Open Questions 2 and 3 (SendGrid subuser-scoped domain authentication mechanics, and whether the live SendGrid account tier even permits subusers).

Purpose: RESEARCH.md rates these MEDIUM/LOW confidence and explicitly says "verify against a live install before writing code that depends on it". Guessing here produces provisioning services that fail at runtime against a real account, which is the most expensive place to discover an error in this phase.
Output: `08-PROVIDER-CONTRACTS.md` — a short, evidence-backed contract sheet that plans 08-08 and 08-09 read before writing a single line of provisioning code.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-RESEARCH.md
@.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-CONTEXT.md

`twilio@6.0.2` is already installed under `apps/service-notifications/node_modules/twilio`. No new dependency is needed to inspect it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the Twilio SDK call chain for subaccount creation and WhatsApp sender registration</name>
  <files>.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md</files>
  <read_first>
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-RESEARCH.md section "Open Questions" item 1 and "Assumptions Log" row A1
    - apps/service-notifications/src/adapters/twilio-whatsapp.adapter.ts lines 1-45 (how the SDK is imported and a client is constructed today: `import twilio from "twilio"` then `twilio(accountSid, authToken)`)
    - apps/service-notifications/package.json (confirm the pinned `twilio` version)
  </read_first>
  <action>
    Inspect the installed SDK's own type definitions rather than documentation. Concretely:

    1. Locate the messaging v2 domain: list `apps/service-notifications/node_modules/twilio/lib/rest/messaging/V2.d.ts` and `apps/service-notifications/node_modules/twilio/lib/rest/messaging/v2/`. Record every exported resource accessor on the v2 domain.
    2. Determine whether the WhatsApp Senders resource is reachable as `client.messaging.v2.channelsSenders`, `client.messaging.v2.channels(...).senders`, or another shape. Record the resolved accessor name verbatim and the file/line that proves it.
    3. Record the exact TypeScript parameter interface name and required fields of the create call (expected to include a sender identifier and a `configuration` object carrying the WABA id), plus the response instance's property names (expected to include a `sid` starting with `XE` and a status field whose values progress `CREATING → OFFLINE → VERIFYING → ONLINE`).
    4. Do the same for subaccount creation: confirm `client.api.v2010.accounts.create` exists, and record its parameter interface (expected `friendlyName`) and which response properties carry the new subaccount's SID and auth token.
    5. Confirm from the type definitions whether a Twilio client can be constructed with an explicit `accountSid` option distinct from the auth credentials (needed to call a subaccount resource from a parent-authenticated client), or whether a fresh `twilio(subaccountSid, subaccountAuthToken)` client is required. RESEARCH.md Pitfall 1 says the Senders API call must use the tenant's subaccount credentials, never the platform client — record which construction achieves that.

    Write the findings into a new `08-PROVIDER-CONTRACTS.md` under a `## Twilio` heading. For each finding record: the claim, the evidence (file path + line), and a confidence label of `VERIFIED` (read from installed type definitions) or `UNVERIFIED` (could not be resolved). If any item cannot be resolved from the installed package, say so explicitly and state what plan 08-08 must do instead — never fabricate a method name.

    Do not install packages, do not call the live Twilio API, and do not put any credential in the document.
  </action>
  <verify>
    <automated>test -f ".planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md" && grep -q "## Twilio" ".planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md"</automated>
  </verify>
  <acceptance_criteria>
    - `08-PROVIDER-CONTRACTS.md` contains a `## Twilio` section naming the resolved Senders accessor verbatim
    - Every Twilio claim in the section is followed by a `node_modules/twilio/...` file path as evidence, or is explicitly labelled `UNVERIFIED`
    - The section states which client construction is required to call the Senders API against a subaccount
    - `grep -ci "AC[0-9a-f]\{32\}\|SK[0-9a-f]\{32\}" ` on the document returns 0 (no real credential was pasted in)
  </acceptance_criteria>
  <done>The Twilio subaccount-create and sender-register call chains are recorded with file-level evidence, or explicitly marked UNVERIFIED with a stated fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the Vapi phone-number import contract and the SendGrid subuser domain-auth call shape</name>
  <files>.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md</files>
  <read_first>
    - .planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-RESEARCH.md "Assumptions Log" rows A2 and A3, and "Open Questions" items 2 and 3
    - apps/service-notifications/src/adapters/vapi-voice.adapter.ts lines 116-205 (existing axios + `Authorization: Bearer` + typed-response pattern against `https://api.vapi.ai`)
    - apps/service-notifications/src/adapters/email.adapter.ts lines 34-56 (existing raw `fetch` + Bearer pattern against SendGrid v3)
  </read_first>
  <action>
    Resolve two external contracts from primary sources and record them in `08-PROVIDER-CONTRACTS.md`.

    Vapi (`## Vapi` section): fetch the current Vapi API reference for creating/importing a phone number (start at `https://docs.vapi.ai/api-reference/phone-numbers/create` and the OpenAPI spec if linked). Record the exact HTTP method and path, the exact request body field names for a Twilio-provider import (RESEARCH.md assumes `provider`, `number`, `twilioAccountSid`, `twilioAuthToken` — confirm or correct), whether an `assistantId` or `serverUrl` may be attached at import time, and the exact response field that carries the persistent id which becomes the per-tenant `vapiPhoneNumberId` (D-05). Note explicitly that authentication uses the platform's `VAPI_API_KEY` (D-04) and that no per-tenant Vapi credential is ever stored.

    SendGrid (`## SendGrid` section): from `https://www.twilio.com/docs/sendgrid/api-reference/subusers-api/create-subuser`, `https://www.twilio.com/docs/sendgrid/api-reference/domain-authentication/`, and `https://www.twilio.com/docs/sendgrid/for-developers/sending-email/automating-subusers/`, record for each of the four provisioning calls (create subuser; create subuser-scoped API key; authenticate a domain; associate an authenticated domain with a subuser) the HTTP method, path, which key authenticates it (parent vs subuser), and whether an `On-Behalf-Of` header is used. Resolve RESEARCH.md Open Question 2 explicitly: state which of the two call shapes produces the CNAME records the tenant must publish, and therefore which one plan 08-09 implements. Also record the documented CNAME record count and the response field that carries the per-record host/value pairs and their `valid` flag.

    Label every claim `CITED` with its source URL, or `UNRESOLVED` with a stated fallback. If a documentation page 404s, say so — do not substitute a search-result summary as if it were the reference.
  </action>
  <verify>
    <automated>grep -q "## Vapi" ".planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md" && grep -q "## SendGrid" ".planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-PROVIDER-CONTRACTS.md"</automated>
  </verify>
  <acceptance_criteria>
    - The `## Vapi` section names the exact HTTP method + path and lists every request body field, plus the response field mapped to `vapiPhoneNumberId`
    - The `## Vapi` section states that authentication is the platform `VAPI_API_KEY` and that no per-tenant Vapi credential is stored (D-04)
    - The `## SendGrid` section has one row per provisioning call with method, path, authenticating key, and On-Behalf-Of usage
    - The `## SendGrid` section contains an explicit resolution sentence for Open Question 2 naming which call shape plan 08-09 implements
    - Every claim carries a source URL or the literal token `UNRESOLVED`
  </acceptance_criteria>
  <done>Vapi import and SendGrid subuser/domain-auth contracts are recorded with source URLs, and Open Question 2 has a stated resolution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm external account prerequisites that only the account owner can see</name>
  <what-built>
    `08-PROVIDER-CONTRACTS.md` now records the verified API contracts for Twilio subaccounts + Senders API, Vapi number import, and SendGrid subusers. Three prerequisites are account/billing facts that cannot be read from code or public docs and that determine whether the *managed* provisioning paths in plans 08-08 and 08-09 can be built and tested at all.
  </what-built>
  <how-to-verify>
    Check each of the following and reply with the answers:

    1. **SendGrid plan tier.** Open the SendGrid dashboard for the account that owns the authenticated `fogging.org` domain → Settings → Account Details (or the billing page). Subusers require Pro Email API, Premier Email API, or Advanced Marketing Campaigns. Report the current plan name. If it is below Pro, the *managed* SendGrid path (plan 08-09) is blocked on a billing change; the BYO SendGrid path is not.

    2. **Twilio Tech Provider (ISV) program status.** Open the Twilio Console → check whether the account is enrolled in the Tech Provider / ISV program and whether Meta Business verification is complete. Report: not started / in review / approved. If not approved, the *managed* WhatsApp path in plan 08-08 cannot be end-to-end tested; the BYO WhatsApp path can.

    3. **Meta app for Embedded Signup.** Confirm whether a Meta app exists with WhatsApp Embedded Signup configured, and if so report the App ID and the Embedded Signup configuration ID (these become `FACEBOOK_APP_ID` and `FACEBOOK_CONFIG_ID`; neither exists in the repo today). If no app exists yet, say so — plan 08-18 will ship the BYO path and the `sdk_unavailable` fallback first and gate the Embedded Signup button behind the presence of these env vars.

    Do not paste any API key, auth token or secret into the reply — only plan names, enrolment status, and the two public Meta identifiers.
  </how-to-verify>
  <acceptance_criteria>
    - `08-PROVIDER-CONTRACTS.md` gains a `## Account Prerequisites` section recording the three answers with the date checked
    - For each of the three, the section states the concrete consequence for plans 08-08, 08-09 and 08-18 (proceed / build BYO path only / gate behind env var)
    - No secret value appears anywhere in the document
  </acceptance_criteria>
  <resume-signal>Reply with the three answers, or type "unknown" for any you cannot check right now — an unknown is recorded as a blocker for the managed path, not as an approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator → planning document | Account facts are transcribed by hand; a pasted secret would land in a git-committed file |
| Public documentation → implementation contract | An unverified doc summary becomes code that talks to a real provider with real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-DOC1 | Information Disclosure | `08-PROVIDER-CONTRACTS.md` committed to git | mitigate | Checkpoint instructions explicitly forbid pasting keys/tokens; acceptance criteria include a grep for credential-shaped strings |
| T-08-DOC2 | Spoofing | fabricated SDK method or endpoint that never existed | mitigate | Every claim must carry file-path or URL evidence, or the literal label `UNVERIFIED`/`UNRESOLVED`; no claim ships unlabelled |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs occur in this plan — inspection is read-only against the already-installed `twilio@6.0.2` |
</threat_model>

<verification>
- `08-PROVIDER-CONTRACTS.md` exists with `## Twilio`, `## Vapi`, `## SendGrid` and `## Account Prerequisites` sections
- Every technical claim is labelled VERIFIED / CITED / UNVERIFIED / UNRESOLVED
- No credential-shaped string appears in the document
</verification>

<success_criteria>
- Plans 08-08 and 08-09 can be executed without guessing a single method name, endpoint or field name
- Any assumption that could not be resolved is visibly flagged with a stated fallback rather than silently carried forward
- The managed-vs-BYO sequencing consequence of each account prerequisite is written down
</success_criteria>

<output>
Create `.planning/phases/08-configuraci-n-por-tenant-byo-canales-e-identidad-de-cobro/08-02-SUMMARY.md` when done. List every item that came back UNVERIFIED/UNRESOLVED — these are inputs to plans 08-08, 08-09 and 08-18.
</output>
