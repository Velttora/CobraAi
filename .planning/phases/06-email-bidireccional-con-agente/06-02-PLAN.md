---
phase: 06-email-bidireccional-con-agente
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts
  - apps/service-notifications/src/webhooks/sendgrid-inbound.handler.spec.ts
  - apps/service-notifications/src/webhooks/webhooks.controller.ts
  - apps/service-notifications/src/webhooks/webhooks.module.ts
autonomous: true
requirements: []
user_setup:
  - service: cloudflare-dns + sendgrid-inbound-parse
    why: "Recibir las respuestas de email del deudor en el webhook (solo necesario para e2e real; el código se construye y testea sin esto)"
    dashboard_config:
      - task: "Agregar registro MX en reply.fogging.org → mx.sendgrid.net (prioridad 10), DNS only"
        location: "Cloudflare DNS → zona fogging.org"
      - task: "Inbound Parse: Add Host & URL → host reply.fogging.org, destination URL https://cobra.fogging.org/api/v1/webhooks/sendgrid-inbound"
        location: "SendGrid → Settings → Inbound Parse"

must_haves:
  truths:
    - "Un POST multipart/form-data a /api/v1/webhooks/sendgrid-inbound con un email de un deudor conocido guarda un Message direction=in channel=email y publica cobrai.email.message_received"
    - "El handler extrae el email del campo from con formato 'Nombre <email@host>'"
    - "Un email cuyo body contiene 'no contactar'/'baja'/'unsubscribe' revoca el ContactConsent de email y NO publica Kafka"
    - "Un email con header Auto-Submitted: auto o from que termina en @reply.fogging.org se ignora (loop prevention) sin publicar Kafka"
    - "Un email cuyo to no pertenece a reply.fogging.org se rechaza sin tocar la BD"
    - "El texto citado del hilo se elimina antes de guardar y publicar"
  artifacts:
    - path: "apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts"
      provides: "SendgridInboundHandler.handleInbound: parsea multipart, identifica deudor por email, opt-out, loop-prevention, guarda Message, publica Kafka"
      exports: ["SendgridInboundHandler", "SendgridInboundPayload"]
      min_lines: 90
    - path: "apps/service-notifications/src/webhooks/sendgrid-inbound.handler.spec.ts"
      provides: "Tests vitest del handler (payload shape, from/envelope parsing, opt-out, loop-skip, dominio destino, limpieza de texto)"
      contains: "SendgridInboundHandler"
    - path: "apps/service-notifications/src/webhooks/webhooks.controller.ts"
      provides: "Endpoint POST sendgrid-inbound con NoFilesInterceptor y @Body() Record<string,string>"
      contains: "sendgrid-inbound"
    - path: "apps/service-notifications/src/webhooks/webhooks.module.ts"
      provides: "SendgridInboundHandler registrado en providers"
      contains: "SendgridInboundHandler"
  key_links:
    - from: "apps/service-notifications/src/webhooks/webhooks.controller.ts"
      to: "apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts"
      via: "this.sendgridInboundHandler.handleInbound(body)"
      pattern: "handleInbound"
    - from: "apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts"
      to: "cobrai.email.message_received"
      via: "this.kafka.publish con channel: email y phone: email"
      pattern: "cobrai.email.message_received"
    - from: "apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts"
      to: "prisma.debtor"
      via: "findFirst where email"
      pattern: "debtor.findFirst"
---

<objective>
Capturar las respuestas de email del deudor. Crear `SendgridInboundHandler` (réplica del patrón `twilio-wa-webhook.handler.ts` para email) y exponer `POST /api/v1/webhooks/sendgrid-inbound` con `NoFilesInterceptor` para parsear el `multipart/form-data` de SendGrid Inbound Parse. El handler identifica al deudor por email, detecta opt-out, previene loops, limpia el texto citado, guarda el mensaje inbound y publica `cobrai.email.message_received`.

Purpose: Hoy el email es solo outbound; las respuestas del deudor se pierden. Este es el lado de "recepción" del canal bidireccional. Es independiente del agente: publica un evento que el Plan 04 cableará al agente. Por eso es Wave 1 (paralelo al EmailAdapter del Plan 01).
Output: handler nuevo + spec nuevo + endpoint en el controller + registro del provider en el módulo.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-email-bidireccional-con-agente/06-CONTEXT.md
@.planning/phases/06-email-bidireccional-con-agente/06-RESEARCH.md
@apps/service-notifications/src/webhooks/twilio-wa-webhook.handler.ts
@apps/service-notifications/src/webhooks/twilio-wa-webhook.handler.spec.ts

<interfaces>
<!-- Contratos que el handler usa. Extraídos del codebase. El executor NO debe explorar. -->

KafkaService.publish (apps/service-notifications/src/kafka/kafka.service.ts):
```typescript
publish<T>(eventType: string, tenantId: string, payload: T): Promise<void>;
```

PrismaService modelos relevantes (packages/db/prisma/schema.prisma — verificado):
- Debtor: { id, tenantId, email?: String (nullable, NO único, NO indexado), deletedAt? }
- Message: { tenantId, conversationId, direction: MessageDirection(in|out), channel: ContactChannel(whatsapp|voice|email|sms|portal), content: String, status: MessageStatus, sentAt? }
- Conversation: { tenantId, debtorId, channel: ContactChannel, status: ConversationStatus(open default), lastMessageAt?, deletedAt? }
- ContactConsent: { tenantId, debtorId, channel: ContactChannel, revokedAt?, deletedAt? }

Constructor del PrismaService importado como `PrismaService` desde `@cobrai/db` (igual que twilio-wa-webhook.handler.ts línea 2).

NoFilesInterceptor (verificado importable desde @nestjs/platform-express@10.4.22, multer 2.0.2 bundled):
```typescript
import { NoFilesInterceptor } from "@nestjs/platform-express";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear SendgridInboundHandler replicando el patrón Twilio para email</name>
  <files>apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts</files>
  <read_first>
    - apps/service-notifications/src/webhooks/twilio-wa-webhook.handler.ts (PATRÓN A REPLICAR punto por punto: estructura `@Injectable`, constructor `(prisma, kafka)`, `handleInbound`, `handleOptOut`, `upsertConversation`, guardado de Message con `content: JSON.stringify(...)`, `conversation.update lastMessageAt`, `kafka.publish`)
    - apps/service-notifications/src/kafka/kafka.service.ts (firma `publish(eventType, tenantId, payload)`)
    - packages/db/prisma/schema.prisma — modelos Debtor/Message/Conversation/ContactConsent y enums ContactChannel(incluye email)/MessageDirection(in|out) (ya extraídos en <interfaces>)
    - 06-RESEARCH.md sección "1. SendGrid Inbound Parse — Payload Exacto" (campos: from, to, subject, text, html, envelope[string JSON], headers[raw])
    - 06-RESEARCH.md sección "Código del SendgridInboundHandler (estructura completa para planificador)" — estructura de referencia completa
    - 06-RESEARCH.md Landmine 2 (envelope es string JSON → JSON.parse en try/catch)
    - 06-RESEARCH.md Landmine 3 (from llega como "Nombre <email>" → extraer con regex `/[\w.+-]+@[\w-]+\.[\w.]+/`)
    - 06-RESEARCH.md Landmine 4 (loop infinito: ignorar si headers incluyen `auto-submitted: auto` o `x-autoreply:`, o si email termina en `@reply.fogging.org`)
    - 06-RESEARCH.md Landmine 8 (text vacío → fallback a html stripped de tags vía `stripHtmlTags`)
  </read_first>
  <action>
    Crear `apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts`. Exportar la interface `SendgridInboundPayload` con campos opcionales: `from?`, `to?`, `subject?`, `text?`, `html?`, `headers?`, `envelope?`, `SPF?`, `spam_score?` (todos string). Exportar la clase `@Injectable() SendgridInboundHandler` con `private readonly logger = new Logger(SendgridInboundHandler.name)` y constructor `(private readonly prisma: PrismaService, private readonly kafka: KafkaService)` importando `PrismaService` desde `@cobrai/db` y `KafkaService` desde `../kafka/kafka.service`.

    Implementar `async handleInbound(payload: SendgridInboundPayload): Promise<void>` en este orden EXACTO:
    1. Validar forma + dominio destino vía un método privado `isValidPayload(payload)`: retorna false (y `handleInbound` retorna) si falta `from` o si faltan ambos `text` y `html`; retorna false si `payload.to ?? ""` NO incluye la subcadena `reply.fogging.org` (loguear warn con el `to` inesperado).
    2. Extraer email del deudor del campo `from` con regex `/[\w.+-]+@[\w-]+\.[\w.]+/`; si no hay match, retornar.
    3. Loop prevention: bajar `payload.headers ?? ""` a lowercase; si incluye `auto-submitted: auto` o `x-autoreply:`, o si el email termina en `@reply.fogging.org`, loguear y retornar SIN procesar.
    4. Obtener cuerpo: `rawBody = payload.text?.trim() || stripHtmlTags(payload.html ?? "")`; luego `body = cleanEmailBody(rawBody)`.
    5. Detectar opt-out con regex case-insensitive `/no\s+contactar|baja|unsubscribe|cancelar|stop|eliminar/i` sobre `body`; si matchea, llamar `handleOptOut(email)` y retornar (NO publicar Kafka, NO guardar Message).
    6. Buscar deudor: `prisma.debtor.findFirst({ where: { email, deletedAt: null } })`; si null, loguear warn y retornar.
    7. Upsert conversación vía método privado `upsertConversation(debtor.tenantId, debtor.id)` (findFirst por `{ tenantId, debtorId, channel: "email", deletedAt: null }`; si no existe, create con `channel: "email", status: "open", lastMessageAt: new Date()`).
    8. Guardar Message: `prisma.message.create` con `data: { tenantId: debtor.tenantId, conversationId: conversation.id, direction: "in", channel: "email", content: JSON.stringify({ text: body, subject: payload.subject }), status: "delivered", sentAt: new Date() }`.
    9. `prisma.conversation.update({ where: { id: conversation.id }, data: { lastMessageAt: new Date() } })`.
    10. Publicar Kafka: `this.kafka.publish("cobrai.email.message_received", debtor.tenantId, { debtor_id: debtor.id, tenant_id: debtor.tenantId, conversation_id: conversation.id, phone: email, body, channel: "email" })`. NOTA: el campo `phone` transporta el email address (reutilizado por compatibilidad con `InboundMessagePayload`; documentar con un comentario inline `// phone reutilizado para email address`).

    Método privado `handleOptOut(email)`: `prisma.debtor.findMany({ where: { email, deletedAt: null } })`; si vacío retornar; `prisma.contactConsent.updateMany({ where: { debtorId: { in: ids }, channel: "email", revokedAt: null, deletedAt: null }, data: { revokedAt: new Date() } })`; loguear.

    Funciones de módulo (fuera de la clase, al final del archivo): `stripHtmlTags(html: string): string` que reemplaza `/<[^>]+>/g` por espacio, colapsa `/\s+/g`, trim y substring(0, 2000); `cleanEmailBody(text: string): string` que divide por `\n`, busca el primer índice de línea que matchee cualquiera de: `/^[-_]{3,}/`, `/^On .+ wrote:/i`, `/^El .+ escribi(ó|o):/i`, `/^Le \w+ \d+ .+ a las/i`, o `l.startsWith(">")`; si el cutoff > 0 corta hasta ahí, si no usa todas las líneas; join con `\n` y trim.

    Si la integración del `envelope` se necesita para algo, envolver `JSON.parse(payload.envelope ?? "{}")` en try/catch (Landmine 2); para v1 el `to` ya se valida desde `payload.to`, así que el `envelope` es opcional — no obligatorio parsearlo. No usar bloques de código fenced; escribir el handler como código TypeScript real en el archivo.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications typecheck</automated>
  </verify>
  <acceptance_criteria>
    - El archivo exporta `SendgridInboundHandler` y `SendgridInboundPayload`: `grep -nE "export (class SendgridInboundHandler|interface SendgridInboundPayload)" apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts` devuelve 2 líneas.
    - Contiene el topic Kafka exacto: `grep -c "cobrai.email.message_received" apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts` ≥ 1.
    - Guarda el mensaje con direction in y channel email: `grep -n 'direction: "in"' …` y `grep -n 'channel: "email"' …` ambos presentes.
    - Loop prevention presente: `grep -niE "auto-submitted|x-autoreply|reply.fogging.org" apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts` muestra las tres guardas.
    - Opt-out presente: la regex incluye `no\s+contactar` y `unsubscribe` (verificable por grep).
    - `pnpm --filter @cobrai/service-notifications typecheck` pasa.
  </acceptance_criteria>
  <done>`SendgridInboundHandler` implementa el flujo completo (validación, parse from/regex, loop-prevention, opt-out, upsert conversación, Message in/email, publish cobrai.email.message_received con phone=email y channel=email); typecheck pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar endpoint POST sendgrid-inbound con NoFilesInterceptor + provider en módulo</name>
  <files>apps/service-notifications/src/webhooks/webhooks.controller.ts, apps/service-notifications/src/webhooks/webhooks.module.ts</files>
  <read_first>
    - apps/service-notifications/src/webhooks/webhooks.controller.ts (el archivo a modificar — patrón `@Post("twilio-whatsapp") @HttpCode(200)` con `@Body() body: Record<string, string>` y `return ""`; constructor que ya inyecta `twilioWaHandler` y `vapiHandler`)
    - apps/service-notifications/src/webhooks/webhooks.module.ts (el archivo a modificar — `providers: [WebhooksService, TwilioWaWebhookHandler, VapiWebhookHandler]`)
    - apps/service-notifications/src/main.ts (confirma `ValidationPipe({ forbidNonWhitelisted: true })` global → POR ESO el body debe ser `Record<string, string>`, NO un DTO class-validator — Landmine 5)
    - 06-RESEARCH.md sección "2. NeJS Multipart/Form-Data" y "Endpoint en WebhooksController" (NoFilesInterceptor activa multer para poblar @Body con campos de texto; SendGrid espera 200 vacío)
    - 06-RESEARCH.md Landmine 1 (sin NoFilesInterceptor el @Body llega vacío y el endpoint no hace nada) y Landmine 5 (no usar DTO class-validator)
  </read_first>
  <action>
    En `webhooks.controller.ts`: agregar imports `import { NoFilesInterceptor } from "@nestjs/platform-express";`, `UseInterceptors` desde `@nestjs/common` (agregar al import existente de `@nestjs/common`), y `import { SendgridInboundHandler } from "./sendgrid-inbound.handler";`. Agregar `private readonly sendgridInboundHandler: SendgridInboundHandler` como nuevo parámetro del constructor (después de `vapiHandler`). Agregar el método:
    - decoradores: `@Post("sendgrid-inbound")`, `@HttpCode(200)`, `@UseInterceptors(NoFilesInterceptor())`
    - firma: `async sendgridInbound(@Body() body: Record<string, string>): Promise<string>`
    - cuerpo: `await this.sendgridInboundHandler.handleInbound(body as never); return "";`
    Usar el tipo `Record<string, string>` para el `@Body()` (NO un DTO con class-validator) para evitar que el `forbidNonWhitelisted` global rechace los campos extra de SendGrid (charsets, attachment-info, etc.). El `return ""` da el 200 vacío que SendGrid espera.

    En `webhooks.module.ts`: agregar `import { SendgridInboundHandler } from "./sendgrid-inbound.handler";` y añadir `SendgridInboundHandler` al array `providers`. NO cambiar `imports` (AdaptersModule/ComplianceModule/KafkaModule/MemoryModule ya están; el handler solo necesita Prisma vía `@cobrai/db` global y KafkaService vía KafkaModule ya importado). No usar bloques de código fenced; editar in-place.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@Post("sendgrid-inbound")' apps/service-notifications/src/webhooks/webhooks.controller.ts` presente.
    - `grep -n "NoFilesInterceptor" apps/service-notifications/src/webhooks/webhooks.controller.ts` presente en import y en `@UseInterceptors(NoFilesInterceptor())`.
    - El handler del endpoint usa `@Body() body: Record<string, string>` (no un DTO): verificable por grep en la firma del método `sendgridInbound`.
    - `grep -c "SendgridInboundHandler" apps/service-notifications/src/webhooks/webhooks.module.ts` ≥ 2 (import + provider).
    - `pnpm --filter @cobrai/service-notifications typecheck` pasa (la DI resuelve el provider).
  </acceptance_criteria>
  <done>Endpoint `POST /api/v1/webhooks/sendgrid-inbound` registrado con `NoFilesInterceptor`, body `Record<string,string>`, retorna 200 vacío; `SendgridInboundHandler` es provider del módulo; typecheck pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests vitest de SendgridInboundHandler</name>
  <files>apps/service-notifications/src/webhooks/sendgrid-inbound.handler.spec.ts</files>
  <read_first>
    - apps/service-notifications/src/webhooks/twilio-wa-webhook.handler.spec.ts (PATRÓN DE TEST a replicar: mocks de prisma/kafka como objetos planos, `handler = new TwilioWaWebhookHandler(mockPrisma as never, mockKafka as never)`, `vi.clearAllMocks()` en beforeEach, asserts con `expect.objectContaining`)
    - apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts (el SUT recién creado — para los nombres exactos de métodos y forma del payload)
    - 06-RESEARCH.md sección "8. Estrategia de Tests → Tests para SendgridInboundHandler" (los 5 casos de referencia: email normal, opt-out, deudor no encontrado, destino fuera de reply.fogging.org, texto limpiado)
    - 06-RESEARCH.md "Phase Requirements → Test Map" (R1–R5, R9, R10 cubiertos por este spec)
  </read_first>
  <behavior>
    - "email normal → deudor encontrado": from "Juan Pérez <juan@test.com>", to "abc@reply.fogging.org", text con cita → `message.create` con `direction: "in", channel: "email"` Y `kafka.publish("cobrai.email.message_received", "org1", objectContaining({ debtor_id: "debtor1", channel: "email" }))`
    - "opt-out 'no contactar'": → `contactConsent.updateMany` con `where` que incluye `channel: "email"` Y `kafka.publish` NO llamado Y `message.create` NO llamado
    - "deudor no encontrado": `debtor.findFirst` → null → resuelve undefined, `kafka.publish` NO llamado
    - "destino fuera de reply.fogging.org": to "cobro@fogging.org" → `debtor.findFirst` NO llamado Y `kafka.publish` NO llamado
    - "loop prevention": headers con "Auto-Submitted: auto" (o from terminando en @reply.fogging.org) → `debtor.findFirst` NO llamado Y `kafka.publish` NO llamado
    - "texto limpiado antes de publicar": text "Pago el viernes.\n\nEl lun 9 jun, CobraAI escribió:\n> Recordatorio..." → el `body` del payload publicado en Kafka es exactamente "Pago el viernes."
    - "conversación no existe → crea nueva": `conversation.findFirst` → null → `conversation.create` con `channel: "email", status: "open"`
  </behavior>
  <action>
    Crear `apps/service-notifications/src/webhooks/sendgrid-inbound.handler.spec.ts` siguiendo EXACTAMENTE la convención de `twilio-wa-webhook.handler.spec.ts`. Declarar mocks: `mockPublish = vi.fn().mockResolvedValue(undefined)`, `mockDebtorFindFirst`, `mockDebtorFindMany`, `mockMessageCreate = vi.fn().mockResolvedValue({ id: "msg1" })`, `mockConversationFindFirst`, `mockConversationCreate = vi.fn().mockResolvedValue({ id: "conv1" })`, `mockConversationUpdate = vi.fn().mockResolvedValue({})`, `mockConsentUpdateMany = vi.fn().mockResolvedValue({ count: 1 })`. Construir `mockPrisma = { debtor: { findFirst: mockDebtorFindFirst, findMany: mockDebtorFindMany }, message: { create: mockMessageCreate }, conversation: { findFirst: mockConversationFindFirst, create: mockConversationCreate, update: mockConversationUpdate }, contactConsent: { updateMany: mockConsentUpdateMany } }` y `mockKafka = { publish: mockPublish }`. En `beforeEach`: `vi.clearAllMocks()` y `handler = new SendgridInboundHandler(mockPrisma as never, mockKafka as never)`.

    Escribir los 7 tests del bloque <behavior>. Para el caso opt-out, `mockDebtorFindMany.mockResolvedValueOnce([{ id: "debtor1" }])` (handleOptOut usa findMany). Para los casos felices, `mockDebtorFindFirst.mockResolvedValueOnce({ id: "debtor1", tenantId: "org1" })` y `mockConversationFindFirst.mockResolvedValueOnce({ id: "conv1" })`. Para "texto limpiado", leer `mockPublish.mock.calls[0]?.[2]?.body` y assert igual a `"Pago el viernes."`. Para "loop prevention", pasar `headers: "Auto-Submitted: auto"` y un `from`/`to` válidos; assert `mockDebtorFindFirst` NO llamado. No usar bloques de código fenced en el plan; el executor escribe el spec real.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications test -- src/webhooks/sendgrid-inbound.handler.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @cobrai/service-notifications test -- src/webhooks/sendgrid-inbound.handler.spec.ts` pasa con ≥7 tests.
    - Existe un test que verifica `message.create` con `expect.objectContaining({ data: expect.objectContaining({ direction: "in", channel: "email" }) })`.
    - Existe un test que verifica `kafka.publish("cobrai.email.message_received", "org1", expect.objectContaining({ debtor_id: "debtor1", channel: "email" }))`.
    - Existe un test de opt-out que verifica `contactConsent.updateMany` con `channel: "email"` y `mockPublish` NO llamado.
    - Existe un test de loop-prevention y uno de destino-inválido que verifican `mockDebtorFindFirst` NO llamado.
    - Existe un test que verifica `mockPublish.mock.calls[0][2].body === "Pago el viernes."`.
  </acceptance_criteria>
  <done>Spec del handler cubre R1–R5, R9, R10 (email normal, opt-out, deudor desconocido, dominio inválido, loop, limpieza, conversación nueva); ≥7 tests pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/v1/webhooks/sendgrid-inbound | Endpoint público sin firma criptográfica (SendGrid Inbound Parse no firma); entrada no confiable controlada por terceros |
| SendgridInboundHandler → BD (Prisma) | El email del remitente determina qué deudor se toca; el body determina opt-out |
| SendgridInboundHandler → Kafka | Publica evento que disparará una respuesta automática del agente (Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Spoofing | POST sendgrid-inbound (sin firma) | accept | SendGrid Inbound Parse no ofrece firma nativa (verificado en research). v1 valida forma + dominio destino (`to` debe contener `reply.fogging.org`) en `isValidPayload`. Verificación criptográfica diferida (CONTEXT.md Deferred). Consistente con webhook Vapi existente. |
| T-06-04 | Denial of Service | loop de auto-respuestas | mitigate | Loop prevention: ignorar `Auto-Submitted: auto`, `X-Autoreply:`, y `from` terminando en `@reply.fogging.org` antes de publicar Kafka. Rate-limit por deudor diferido a v2 (research §Landmine 4). |
| T-06-05 | Tampering | body multipart con campos extra | mitigate | `@Body() Record<string,string>` (no DTO class-validator) evita que `forbidNonWhitelisted` rompa, y `NoFilesInterceptor` rechaza adjuntos de archivo con BadRequestException. El handler solo lee campos conocidos. |
| T-06-06 | Information Disclosure | deudor equivocado por email duplicado | accept | `Debtor.email` no es único; `findFirst` podría asignar la respuesta al deudor equivocado si dos comparten email (research A4, riesgo bajo en v1). Índice/unicidad diferido a producción. |
| T-06-SC | Tampering | npm installs | mitigate | Este plan NO instala paquetes (NoFilesInterceptor ya en @nestjs/platform-express@10.4.22 con multer 2.0.2 bundled, verificado). Sin nuevas dependencias → sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `pnpm --filter @cobrai/service-notifications typecheck` pasa (handler + endpoint + DI).
- `pnpm --filter @cobrai/service-notifications test -- src/webhooks/sendgrid-inbound.handler.spec.ts` pasa (≥7 tests).
- `grep -c "cobrai.email.message_received" apps/service-notifications/src/webhooks/sendgrid-inbound.handler.ts` ≥ 1.
- El endpoint usa `NoFilesInterceptor` y body `Record<string,string>`.
</verification>

<success_criteria>
- Un POST multipart con email de deudor conocido → Message(in,email) guardado + `cobrai.email.message_received` publicado con `phone=email` y `channel=email`.
- Opt-out, loop-prevention y validación de dominio destino bloquean la publicación cuando corresponde.
- Texto citado eliminado antes de persistir/publicar.
- `SendgridInboundHandler` registrado como provider; endpoint accesible.
</success_criteria>

<output>
Create `.planning/phases/06-email-bidireccional-con-agente/06-02-SUMMARY.md` when done
</output>
