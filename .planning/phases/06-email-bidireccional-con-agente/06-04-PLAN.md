---
phase: 06-email-bidireccional-con-agente
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - apps/service-notifications/src/contacts/kafka.consumer.ts
  - apps/service-notifications/src/contacts/kafka.consumer.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "El consumer está suscrito al topic cobrai.email.message_received"
    - "Al recibir cobrai.email.message_received el consumer despacha al agente via processInboundMessage con el payload (que ya incluye channel: email)"
    - "El despacho de cobrai.whatsapp.message_received sigue funcionando igual (sin regresión)"
  artifacts:
    - path: "apps/service-notifications/src/contacts/kafka.consumer.ts"
      provides: "CONSUMED_TOPICS incluye cobrai.email.message_received; dispatch tiene case que llama agent.processInboundMessage"
      contains: "cobrai.email.message_received"
    - path: "apps/service-notifications/src/contacts/kafka.consumer.spec.ts"
      provides: "Test que verifica que el case email despacha al agente"
      contains: "cobrai.email.message_received"
  key_links:
    - from: "apps/service-notifications/src/contacts/kafka.consumer.ts"
      to: "apps/service-notifications/src/agent/conversation-agent.service.ts"
      via: "case cobrai.email.message_received → this.agent.processInboundMessage(payload)"
      pattern: "processInboundMessage"
---

<objective>
Cerrar el lazo bidireccional: cablear el evento `cobrai.email.message_received` (publicado por el `SendgridInboundHandler` del Plan 02) al `ConversationAgentService.processInboundMessage` (generalizado a multi-canal en el Plan 03). Agregar el topic a `CONSUMED_TOPICS` y un `case` en el `dispatch` del `KafkaConsumerService`.

Purpose: Sin este wiring, el inbound handler publica el evento pero nadie lo consume — el agente nunca responde por email. Este plan es Wave 3 porque depende del topic (Plan 02) y del agente multi-canal (Plan 03). El payload ya trae `channel: "email"` desde el handler, así que el agente responde por el canal correcto automáticamente.
Output: consumer suscrito y despachando el evento de email + spec nuevo del consumer.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-email-bidireccional-con-agente/06-CONTEXT.md
@.planning/phases/06-email-bidireccional-con-agente/06-RESEARCH.md
@apps/service-notifications/src/contacts/kafka.consumer.ts

<interfaces>
<!-- Contratos que el consumer usa. Extraídos del codebase. -->

ConversationAgentService (apps/service-notifications/src/agent/conversation-agent.service.ts, tras Plan 03):
```typescript
processInboundMessage(payload: InboundMessagePayload): Promise<void>;
// InboundMessagePayload incluye channel?: "whatsapp" | "email"; el payload de email ya trae channel: "email"
```

KafkaConsumerService.dispatch (apps/service-notifications/src/contacts/kafka.consumer.ts):
```typescript
private dispatch(topic: string, envelope: KafkaEventEnvelope): Promise<void>;
// envelope.tenant_id, envelope.payload as Record<string, unknown>
// switch(topic) con cases existentes: cobrai.contact.requested, cobrai.whatsapp.message_received, cobrai.voice.call_completed
```

El case existente de whatsapp (líneas 81-85) es el patrón a replicar para email:
```typescript
case "cobrai.whatsapp.message_received":
  await this.agent.processInboundMessage(payload as unknown as InboundMessagePayload);
  break;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suscribir y despachar cobrai.email.message_received al agente</name>
  <files>apps/service-notifications/src/contacts/kafka.consumer.ts</files>
  <read_first>
    - apps/service-notifications/src/contacts/kafka.consumer.ts (el archivo a modificar — `CONSUMED_TOPICS` líneas 13-17; `dispatch` switch líneas 74-96; el case whatsapp 81-85 es el patrón a replicar; `InboundMessagePayload` ya está importado en la línea 9-11)
    - apps/service-notifications/src/agent/conversation-agent.service.ts (confirmar firma `processInboundMessage(payload)` y que `InboundMessagePayload` ahora tiene `channel?`)
    - 06-RESEARCH.md sección "6. Kafka Consumer — Delta Mínimo" (los dos cambios exactos: agregar al array + agregar al switch)
  </read_first>
  <action>
    En `apps/service-notifications/src/contacts/kafka.consumer.ts`:
    1. Agregar `"cobrai.email.message_received"` como nuevo elemento del array `CONSUMED_TOPICS` (después de `"cobrai.voice.call_completed"`).
    2. En el `switch (topic)` dentro de `dispatch`, agregar un `case "cobrai.email.message_received":` que llame `await this.agent.processInboundMessage(payload as unknown as InboundMessagePayload); break;` — idéntico al case de whatsapp. El payload ya incluye `channel: "email"` (lo pone el SendgridInboundHandler), por lo que NO hay que inyectar el canal aquí.
    NO cambiar el constructor (el `agent: ConversationAgentService` ya está inyectado). NO tocar los otros cases. No usar bloques de código fenced; editar in-place.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cobrai.email.message_received" apps/service-notifications/src/contacts/kafka.consumer.ts` ≥ 2 (en CONSUMED_TOPICS + en el case del switch).
    - El nuevo case llama `this.agent.processInboundMessage`: verificable por grep en el bloque del case email.
    - Los cases preexistentes (contact.requested, whatsapp.message_received, voice.call_completed) permanecen sin cambios.
    - `pnpm --filter @cobrai/service-notifications typecheck` pasa.
  </acceptance_criteria>
  <done>`cobrai.email.message_received` está en `CONSUMED_TOPICS` y su `case` despacha a `agent.processInboundMessage`; typecheck pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test del dispatch de email en kafka.consumer.spec.ts</name>
  <files>apps/service-notifications/src/contacts/kafka.consumer.spec.ts</files>
  <read_first>
    - apps/service-notifications/src/contacts/kafka.consumer.ts (el SUT — `dispatch` es privado; el test lo invoca vía `(consumer as never as { dispatch: Function }).dispatch(topic, envelope)` o exponiendo el método; revisar si `dispatch` se puede llamar por acceso a propiedad)
    - apps/service-notifications/src/webhooks/twilio-wa-webhook.handler.spec.ts (convención vitest del proyecto: mocks planos, `new Clase(mocks as never)`, `vi.clearAllMocks`)
    - apps/service-notifications/src/agent/conversation-agent.service.spec.ts (referencia adicional de convención de mocks)
    - 06-RESEARCH.md "Phase Requirements → Test Map" R6 (Kafka consumer despacha al agente con channel:email — Unit, kafka.consumer.spec.ts o agregar a spec existente)
  </read_first>
  <behavior>
    - Test "dispatch cobrai.email.message_received → llama agent.processInboundMessage con el payload": construir un envelope `{ tenant_id: "org1", payload: { debtor_id: "debtor1", tenant_id: "org1", conversation_id: "conv1", phone: "juan@test.com", body: "Hola", channel: "email" } }` → `mockAgent.processInboundMessage` llamado con un objeto que incluye `channel: "email"`
    - Test "dispatch cobrai.whatsapp.message_received → sigue llamando agent.processInboundMessage" (anti-regresión)
  </behavior>
  <action>
    Crear `apps/service-notifications/src/contacts/kafka.consumer.spec.ts`. Instanciar `KafkaConsumerService` con mocks: `mockConfig` (un objeto con `get: vi.fn(() => undefined)` para que `onModuleInit` no conecte a Kafka — pero el test NO llama `onModuleInit`, solo `dispatch`), `mockContacts = { handleContactRequested: vi.fn() }`, `mockAgent = { processInboundMessage: vi.fn().mockResolvedValue(undefined) }`. Construir `consumer = new KafkaConsumerService(mockConfig as never, mockContacts as never, mockAgent as never)`. Como `dispatch` es privado, invocarlo vía cast: `await (consumer as unknown as { dispatch(topic: string, envelope: unknown): Promise<void> }).dispatch("cobrai.email.message_received", { tenant_id: "org1", payload: { debtor_id: "debtor1", tenant_id: "org1", conversation_id: "conv1", phone: "juan@test.com", body: "Hola", channel: "email" } })`. Assert `expect(mockAgent.processInboundMessage).toHaveBeenCalledWith(expect.objectContaining({ debtor_id: "debtor1", channel: "email" }))`. Agregar el test anti-regresión para `cobrai.whatsapp.message_received` que verifica que `mockAgent.processInboundMessage` también se llama. Usar `vi.clearAllMocks()` en `beforeEach`. No usar bloques de código fenced; el executor escribe el spec real.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications test -- src/contacts/kafka.consumer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @cobrai/service-notifications test -- src/contacts/kafka.consumer.spec.ts` pasa con ≥2 tests.
    - Existe un test que invoca `dispatch("cobrai.email.message_received", …)` y verifica `mockAgent.processInboundMessage` llamado con `expect.objectContaining({ channel: "email" })`.
    - Existe un test anti-regresión que verifica el dispatch de `cobrai.whatsapp.message_received` al agente.
    - El test NO requiere conexión real a Kafka (no llama `onModuleInit`).
  </acceptance_criteria>
  <done>El spec del consumer verifica que el evento de email se despacha al agente con `channel: "email"` y que WhatsApp no se rompe; ≥2 tests pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka topic → KafkaConsumerService | Eventos consumidos; el `dispatch` enruta al handler correcto por topic |
| KafkaConsumerService → ConversationAgentService | Despacho del payload de email al agente, que responderá automáticamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | payload del evento Kafka | accept | El evento es publicado internamente por el SendgridInboundHandler (Plan 02) que ya validó dominio/loop/opt-out; el consumer confía en el bus interno. Consistente con el case whatsapp existente. |
| T-06-10 | Denial of Service | volumen de eventos email | accept | El loop-prevention vive aguas arriba (Plan 02). El consumer procesa secuencialmente como ya hace para whatsapp/voice; rate-limit por deudor diferido a v2 (research §Landmine 4). |

Sin instalación de paquetes en este plan → T-06-SC no aplica.
</threat_model>

<verification>
- `pnpm --filter @cobrai/service-notifications typecheck` pasa.
- `pnpm --filter @cobrai/service-notifications test -- src/contacts/kafka.consumer.spec.ts` pasa (≥2 tests).
- `grep -c "cobrai.email.message_received" apps/service-notifications/src/contacts/kafka.consumer.ts` ≥ 2.
- Suite completa de regresión: `pnpm --filter @cobrai/service-notifications test` pasa (todos los specs del servicio).
</verification>

<success_criteria>
- El consumer se suscribe a `cobrai.email.message_received` y lo despacha a `agent.processInboundMessage` con el payload (que trae `channel: "email"`).
- Sin regresión en el despacho de WhatsApp/voz.
- Lazo bidireccional completo: deudor responde email → handler captura/publica → consumer despacha → agente responde por email con memoria unificada.
</success_criteria>

<output>
Create `.planning/phases/06-email-bidireccional-con-agente/06-04-SUMMARY.md` when done
</output>
