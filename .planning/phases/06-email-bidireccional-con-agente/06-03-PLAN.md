---
phase: 06-email-bidireccional-con-agente
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - apps/service-notifications/src/agent/conversation-agent.service.ts
  - apps/service-notifications/src/agent/conversation-agent.service.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "processInboundMessage acepta channel ('whatsapp' | 'email') en el payload, con default 'whatsapp' cuando está ausente"
    - "Cuando channel es 'email' el agente responde vía EmailAdapter.sendTemplate con reply_to fijo a reply@reply.fogging.org y NO usa WhatsApp"
    - "Cuando channel es 'whatsapp' (o ausente) el agente responde vía TwilioWhatsAppAdapter y NO usa EmailAdapter (sin regresión WhatsApp)"
    - "El mensaje outbound se guarda con el channel correcto"
    - "Cuando channel es 'email' e intent es opt_out, se revoca el ContactConsent del canal email (no whatsapp) — compliance Ley 1266"
    - "El agente sigue usando getUnifiedContext (Phase 5) para construir el prompt"
  artifacts:
    - path: "apps/service-notifications/src/agent/conversation-agent.service.ts"
      provides: "InboundMessagePayload con channel?; constructor inyecta EmailAdapter; respuesta discriminada por canal; outbound message con channel dinámico"
      contains: "channel"
    - path: "apps/service-notifications/src/agent/conversation-agent.service.spec.ts"
      provides: "Tests multi-canal (email→EmailAdapter, whatsapp→WhatsApp) + setup con mockEmail como 6° arg"
      contains: "mockEmail"
  key_links:
    - from: "apps/service-notifications/src/agent/conversation-agent.service.ts"
      to: "apps/service-notifications/src/adapters/email.adapter.ts"
      via: "this.email.sendTemplate con reply_to cuando channel === email"
      pattern: "this.email.sendTemplate"
    - from: "apps/service-notifications/src/agent/conversation-agent.service.ts"
      to: "apps/service-notifications/src/adapters/twilio-whatsapp.adapter.ts"
      via: "this.whatsapp.sendTemplate cuando channel !== email"
      pattern: "this.whatsapp.sendTemplate"
---

<objective>
Generalizar `ConversationAgentService` para que responda por el canal correcto. Hoy `processInboundMessage` asume WhatsApp (todos los `channel: "whatsapp"` hardcodeados, responde por `this.whatsapp.sendTemplate`). Agregar `channel?: "whatsapp" | "email"` a `InboundMessagePayload`, inyectar `EmailAdapter` como 6° parámetro del constructor, discriminar el adapter de salida por canal (email lleva `reply_to: reply@reply.fogging.org`), y guardar el mensaje outbound con el canal correcto. Mantener intactos `getUnifiedContext` y todo el manejo de intents.

Purpose: Es el corazón de la bidireccionalidad por email. Reutiliza toda la lógica de LLM/intents/memoria ya existente; el delta es el discriminador de canal. Depende del Plan 01 porque la rama email llama a `email.sendTemplate({ ..., reply_to })` y necesita que `EmailAdapter` realmente envíe ese `reply_to` a SendGrid.
Output: agente multi-canal + spec actualizado con `mockEmail` y dos tests de discriminación de canal. WhatsApp sin regresión.
</objective>

<execution_context>
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gustavo.moreno/Documents/personal info/Renova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-email-bidireccional-con-agente/06-CONTEXT.md
@.planning/phases/06-email-bidireccional-con-agente/06-RESEARCH.md
@apps/service-notifications/src/agent/conversation-agent.service.ts
@apps/service-notifications/src/agent/conversation-agent.service.spec.ts

<interfaces>
<!-- Contratos que el agente usa. EmailAdapter y WhatsApp comparten la misma forma de input. -->

EmailAdapter.sendTemplate (apps/service-notifications/src/adapters/email.adapter.ts) — input @cobrai/ports SendEmailTemplateInput:
```typescript
sendTemplate(input: {
  to: string;
  template_id: string;
  variables: Record<string, string>;
  tenant_id: string;
  reply_to?: string;   // cableado en Plan 01
}): Promise<{ message_id: string; status: "sent" | "failed" }>;
```

TwilioWhatsAppAdapter.sendTemplate — input SendWhatsAppTemplateInput (misma forma SIN reply_to):
```typescript
sendTemplate(input: {
  to: string; template_id: string; variables: Record<string, string>; tenant_id: string;
}): Promise<{ message_id: string; status: "sent" | "failed" }>;
```

AgentModule ya importa AdaptersModule que exporta EmailAdapter (verificado: agent.module.ts + adapters.module.ts) → inyectar EmailAdapter en el constructor NO requiere cambiar AgentModule.

Constante de Reply-To para v1 (Claude's Discretion resuelto en research §4): dirección FIJA `reply@reply.fogging.org`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalizar processInboundMessage a multi-canal + inyectar EmailAdapter</name>
  <files>apps/service-notifications/src/agent/conversation-agent.service.ts</files>
  <read_first>
    - apps/service-notifications/src/agent/conversation-agent.service.ts (el archivo a modificar — interface `InboundMessagePayload` líneas 11-18; constructor líneas 47-63 con 5 params actuales; guardado outbound `channel: "whatsapp"` línea ~169; envío `this.whatsapp.sendTemplate` líneas 181-188; intents en `applyIntent`)
    - apps/service-notifications/src/adapters/email.adapter.ts (forma de `sendTemplate` y que ya pasa `reply_to` tras Plan 01)
    - apps/service-notifications/src/adapters/twilio-whatsapp.adapter.ts (referencia del otro adapter)
    - apps/service-notifications/src/agent/agent.module.ts (confirmar que importa AdaptersModule → no se cambia el módulo)
    - 06-RESEARCH.md sección "5. Generalización del Agente a Multi-Canal — Diff Mínimo" (los cambios exactos en pasos 5, 6, 7 del método)
    - 06-RESEARCH.md sección "4. Reply-To" (constante fija `reply@reply.fogging.org` para la rama email)
    - 06-RESEARCH.md Open Question #1 (el campo `phone` se reutiliza para el email address — documentar en el interface; NO renombrar)
  </read_first>
  <behavior>
    - `InboundMessagePayload` gana `channel?: "whatsapp" | "email"` (opcional, default "whatsapp")
    - Con `payload.channel === "email"`: se llama `this.email.sendTemplate` con `to: phone`, `template_id: "agent_response"`, `variables: { body: agentResponse.response }`, `tenant_id`, `reply_to: "reply@reply.fogging.org"`; NO se llama `this.whatsapp.sendTemplate`
    - Con `payload.channel` ausente o "whatsapp": se llama `this.whatsapp.sendTemplate` (forma actual); NO se llama `this.email.sendTemplate`
    - El Message outbound se guarda con `channel: payload.channel ?? "whatsapp"`
    - `intent === "opt_out"` sigue NO enviando ningún mensaje (ni email ni WhatsApp), y revoca el ContactConsent del canal correcto (`payload.channel ?? "whatsapp"`) — un opt-out por email revoca consent de email, no de whatsapp
    - `getUnifiedContext` se sigue llamando con `(tenant_id, debtor_id, debt.id)` igual que hoy
  </behavior>
  <action>
    Importar `EmailAdapter` desde `../adapters/email.adapter`. En la interface `InboundMessagePayload`, agregar `channel?: "whatsapp" | "email";` con un comentario inline indicando default "whatsapp" si ausente, y un comentario en el campo `phone` aclarando que para email transporta el email address del deudor (reutilizado por compatibilidad). Agregar `private readonly email: EmailAdapter` como 6° parámetro del constructor (después de `debtorMemory`).

    Definir una constante de módulo `const EMAIL_REPLY_TO = "reply@reply.fogging.org";` (Claude's Discretion: dirección fija para v1).

    En el paso de guardado del Message outbound (`prisma.message.create`, hoy `channel: "whatsapp"`), cambiar a `channel: payload.channel ?? "whatsapp"`.

    En el paso de envío (hoy bloque `if (agentResponse.intent !== "opt_out") { await this.whatsapp.sendTemplate(...) }`), mantener la guarda de `opt_out` y dentro discriminar por canal: si `(payload.channel ?? "whatsapp") === "email"`, llamar `this.email.sendTemplate({ to: phone, template_id: "agent_response", variables: { body: agentResponse.response }, tenant_id, reply_to: EMAIL_REPLY_TO })`; en caso contrario, llamar `this.whatsapp.sendTemplate({ to: phone, template_id: "agent_response", variables: { body: agentResponse.response }, tenant_id })` (forma actual). Recordar que `phone` ya está desestructurado del payload y para email contiene el email address.

    REQUERIDO (compliance Ley 1266 — corrige bug de canal en opt-out): propagar el canal a `applyIntent` añadiendo `channel` al objeto `ctx` que se le pasa. En el case `opt_out` de `applyIntent`, el `contactConsent.updateMany` hoy hardcodea `channel: "whatsapp"`; cambiarlo a `channel: ctx.channel ?? "whatsapp"` para que un opt-out recibido por email revoque el consent de email (no de WhatsApp). Por consistencia downstream, en los `kafka.publish` de `applyIntent` que hoy hardcodean `channel: "whatsapp"` (promise_registered, disputed, escalation.requested) usar también `ctx.channel ?? "whatsapp"`. Mantener el resto de la lógica de intents igual (los `updateMany` de status de debt/conversation permanecen). NO usar bloques de código fenced; editar el servicio in-place.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'channel?: "whatsapp" | "email"' apps/service-notifications/src/agent/conversation-agent.service.ts` presente en la interface.
    - El constructor inyecta EmailAdapter: `grep -n "email: EmailAdapter" apps/service-notifications/src/agent/conversation-agent.service.ts` presente.
    - Rama email presente: `grep -n "this.email.sendTemplate" …` y `grep -n "reply@reply.fogging.org" …` ambos presentes.
    - Rama whatsapp preservada: `grep -n "this.whatsapp.sendTemplate" …` presente.
    - Outbound dinámico: `grep -n 'channel: payload.channel' apps/service-notifications/src/agent/conversation-agent.service.ts` presente.
    - `grep -n "getUnifiedContext" …` sigue presente (memoria Phase 5 intacta).
    - opt_out channel-aware: `grep -n "ctx.channel" apps/service-notifications/src/agent/conversation-agent.service.ts` presente (el opt_out y los publishes usan el canal correcto, no "whatsapp" hardcodeado).
    - `pnpm --filter @cobrai/service-notifications typecheck` pasa.
  </acceptance_criteria>
  <done>`processInboundMessage` discrimina adapter por `channel` (email con reply_to fijo, whatsapp default), guarda outbound con channel dinámico, mantiene getUnifiedContext e intents; typecheck pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests multi-canal en conversation-agent.service.spec.ts</name>
  <files>apps/service-notifications/src/agent/conversation-agent.service.spec.ts</files>
  <read_first>
    - apps/service-notifications/src/agent/conversation-agent.service.spec.ts (el spec a modificar — `mockWhatsapp` ya existe líneas 35-37; el servicio se instancia con 5 args líneas 132-138; `basePayload` líneas 81-87; mocks de prisma/kafka/debtorMemory ya configurados)
    - apps/service-notifications/src/agent/conversation-agent.service.ts (SUT modificado — para confirmar el 6° arg EmailAdapter y los nombres exactos)
    - 06-RESEARCH.md sección "8. Estrategia de Tests → Tests para ConversationAgentService (multi-canal)" (los 2 tests de referencia + el mockEmail como 6° arg)
    - 06-RESEARCH.md Landmine 6 (agregar EmailAdapter como 6° param ROMPE el spec existente que instancia con 5 args → hay que actualizar la instanciación Y agregar mockEmail)
  </read_first>
  <behavior>
    - Setup: agregar `mockEmail = { sendTemplate: vi.fn().mockResolvedValue({ message_id: "em1", status: "sent" }) }` y pasarlo como 6° argumento del constructor en `beforeEach` (sin esto el spec existente deja de compilar/correr)
    - Test "channel email → EmailAdapter, NO whatsapp": `processInboundMessage({ ...basePayload, phone: "juan@test.com", channel: "email" })` → `mockEmail.sendTemplate` llamado con `objectContaining({ to: "juan@test.com", reply_to: "reply@reply.fogging.org" })` Y `mockWhatsapp.sendTemplate` NO llamado
    - Test "channel whatsapp (default) → WhatsApp, NO email": `processInboundMessage(basePayload)` (sin channel) → `mockWhatsapp.sendTemplate` llamado Y `mockEmail.sendTemplate` NO llamado
    - Test "opt_out por email revoca consent de email (no whatsapp)": con el LLM mockeado devolviendo `intent: "opt_out"` y `processInboundMessage({ ...basePayload, phone: "juan@test.com", channel: "email" })` → `mockPrisma.contactConsent.updateMany` llamado con `where` que incluye `channel: "email"` (no "whatsapp")
    - Los 11 tests existentes siguen pasando (sin regresión WhatsApp)
  </behavior>
  <action>
    En el spec existente, agregar la constante `mockEmail` (junto a `mockWhatsapp`) con `sendTemplate: vi.fn().mockResolvedValue({ message_id: "em1", status: "sent" })`. En `beforeEach`, actualizar la instanciación `service = new ConversationAgentService(makeConfig(), mockPrisma as never, mockKafka as never, mockWhatsapp as never, mockDebtorMemory as never, mockEmail as never)` agregando `mockEmail as never` como 6° argumento. Agregar los TRES tests del bloque <behavior> (email→EmailAdapter, whatsapp default→WhatsApp, opt_out email→revoca consent email) al `describe` existente sin reescribir los tests previos. Para el test de opt_out email, mockear el LLM para devolver `intent: "opt_out"` y assert `expect(mockPrisma.contactConsent.updateMany).toHaveBeenCalledWith(expect.objectContaining({ where: expect.objectContaining({ channel: "email" }) }))`. Para el test de email, sobrescribir `phone` a un email y pasar `channel: "email"`; assert `mockEmail.sendTemplate` con `expect.objectContaining({ to: "juan@test.com", reply_to: "reply@reply.fogging.org" })` y `expect(mockWhatsapp.sendTemplate).not.toHaveBeenCalled()`. Para el test default, usar `basePayload` tal cual y assert `expect(mockWhatsapp.sendTemplate).toHaveBeenCalled()` y `expect(mockEmail.sendTemplate).not.toHaveBeenCalled()`. NO usar bloques de código fenced; el executor escribe los tests.
  </action>
  <verify>
    <automated>cd "/Users/gustavo.moreno/Documents/personal info/Renova" && pnpm --filter @cobrai/service-notifications test -- src/agent/conversation-agent.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @cobrai/service-notifications test -- src/agent/conversation-agent.service.spec.ts` pasa con ≥14 tests (11 existentes + 3 nuevos).
    - `grep -c "mockEmail" apps/service-notifications/src/agent/conversation-agent.service.spec.ts` ≥ 3 (declaración + 6° arg + asserts).
    - El test de email verifica `mockEmail.sendTemplate` con `reply_to: "reply@reply.fogging.org"` y `mockWhatsapp.sendTemplate` NO llamado.
    - El test default verifica `mockWhatsapp.sendTemplate` llamado y `mockEmail.sendTemplate` NO llamado.
    - El test de opt_out por email verifica `contactConsent.updateMany` con `channel: "email"` en el `where`.
    - Los tests WhatsApp preexistentes (promise_to_pay, dispute, escalate, opt_out, fallback, etc.) siguen verdes.
  </acceptance_criteria>
  <done>El spec instancia el servicio con `mockEmail` como 6° arg y verifica la discriminación de canal en ambas direcciones; ≥13 tests pasan sin regresión WhatsApp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConversationAgentService → EmailAdapter/WhatsAppAdapter | El `channel` del payload decide a dónde sale la respuesta auto-generada por LLM |
| ConversationAgentService → OpenAI | Sin cambios respecto a Phase 3/5 (mismo prompt + getUnifiedContext) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Spoofing | channel del payload | accept | El `channel` proviene de un publisher interno (SendgridInboundHandler / TwilioWaWebhookHandler), no de entrada directa del deudor. El default "whatsapp" evita comportamiento indefinido si falta. |
| T-06-08 | Information Disclosure | respuesta enviada al canal equivocado | mitigate | Discriminación explícita por `channel` con default seguro; el `to` (phone/email) viene del mismo payload que el canal, evitando cruces. Tests verifican que email NUNCA dispara WhatsApp y viceversa. |
| T-06-04 (ref) | Denial of Service | loop de email | mitigate | La prevención de loop vive en el inbound handler (Plan 02); el agente solo responde a eventos ya filtrados. El `reply_to` fijo a dominio propio mantiene el hilo controlado. |

Sin instalación de paquetes en este plan → T-06-SC no aplica.
</threat_model>

<verification>
- `pnpm --filter @cobrai/service-notifications typecheck` pasa.
- `pnpm --filter @cobrai/service-notifications test -- src/agent/conversation-agent.service.spec.ts` pasa (≥13 tests).
- Rama email usa `reply@reply.fogging.org`; rama whatsapp intacta (grep).
</verification>

<success_criteria>
- `processInboundMessage` responde por EmailAdapter (con reply_to) para email y por WhatsApp para whatsapp/default, sin cruzar canales.
- Outbound Message persistido con el channel correcto.
- getUnifiedContext e intents intactos; sin regresión en los tests WhatsApp existentes.
</success_criteria>

<output>
Create `.planning/phases/06-email-bidireccional-con-agente/06-03-SUMMARY.md` when done
</output>
